# 模块框图与停靠改造

开始日期：2026-09-22。状态：17 项已实现，Ela 针对性回归通过，纳入 0.31.0。

本轮按 README 中的 17 项需求实施，代码通过 `apply_patch` 修改。
用户已授权提交、推送及更新正式包；仅发布 Ela，不生成 ZIP。

## 实施边界

- “全部模块”指当前顶层及其所有子模块，不限制展示层级，不加入其他独立模块。
- 保留嵌套框图与其他专业图的独立渲染。共享源码导航、生命周期和持久化能力继续复用。
- 图内导航使用双击与鼠标侧键；悬浮使用高亮和标准箭头。
- 悬浮、侧栏和底栏之间搬移同一内容实例，多面板同时可见并可拖动分隔条调尺寸。

## 进度

- [x] 1–2：移除 Follow Editor、Pin/Pinned 控件及独立开关状态，处理旧状态兼容。
- [x] 3、7：内容占满视图；精简顶栏并提供 Fit。
- [x] 4–6：删除模块缩放按钮、搜索和更多菜单及专用逻辑。
- [x] 8–10：全部层级、不可达虚线、同层级配色。
- [x] 11–16：悬浮反馈、双击与源码联动、父级虚化、侧键历史、移除三角和手形。
- [x] 17：侧栏／底栏多面板停靠、拖放和尺寸调整，保存与恢复布局。
- [x] 更新使用说明，完成 Ela 相关回归。

用户于实施期间明确：从现在起仅维护 Ela。本轮后续验证和发布目标均采用 Ela，停止继续维护 classic 路径。

## 验收

覆盖深层与循环实例、不可解析模块、重复实例、窄窗与放大、缩放与 Fit、导航历史分叉、
源码跳转及文档切换、面板来回停靠的内容身份、布局持久化及旧工作区恢复。
桌面鼠标验证前提醒用户；后台或隐藏窗口测试不等同于视觉验收。

Ela Release 构建通过，26 项针对性 CTest 全部通过（40.56 秒），覆盖：

- 模块完整层级、不可达分支、循环边界、双击定义跳转、历史分叉和显式目标保留。
- 模块画布在 125%／150%／200% 下的布局、Fit 与浮窗／底栏／侧栏往返。
- 两区域多面板同时可见、相邻分隔条调尺寸、同一内容实例及 v7 持久化。
- 上下文视图生命周期、相关专业图、共享导航、动作注册和四档 Ela 面板动效。

补充回归定位并修复了视图析构时语义通知重入导致的崩溃，以及底栏首次显示时临时尺寸
覆盖用户高度和隐藏工具行残留间距的问题。
本地证据：`build/ela-migration/module-final-build.log`、`module-final-tests.log`，
布局截图位于同目录 `module-review/`。本轮未操控桌面鼠标，原生拖放、混合 DPI 与窗口管理器
行为仍按 [手工检查清单](unverified-manual-checks.md) 保留为未验项。

## 行为与兼容性

2026-09-23 后续精简（0.31.4）：移除模块框图停靠标题条的折叠箭头、Fit 右侧的完整视图图标，
以及独立的实例面包屑／前进回退行。鼠标侧键仍浏览历史；右侧功能按钮仍可收起／恢复面板，旧折叠状态恢复为展开。
画布去掉外层边框并贴合面板，主题切换后继续生效。上下文浮窗使用 Ela 无系统边框外壳，
移除外层内容留白，窗口按钮、拖动、边缘缩放和几何恢复仍由 Ela 与原资源控制器处理。
其他专业图的专用工具和画布样式保留。
Ela 第 21 份兼容补丁补齐无边框窗口四边和四角的 DPI 缩放命中，以及最大化时的显示器工作区边界；
保留上游 MIT／字体 OFL 许可证和补丁来源记录。

本次 Ela Release 构建及 10 项针对性 CTest 全部通过（17.08 秒），覆盖专业图导航、窗口与视图生命周期、
旧布局恢复、主题切换及 100%／125%／150%／200% 布局。模块框图另以 200% 运行停靠／悬浮和导航用例，
核对 100% 与 200% 后台截图。隐藏原生窗口检查通过，验证无边框客户区及四边、四角缩放命中，
并确认窗口未显示、前台焦点未改变；未进行桌面鼠标操作验收。
记录：`build/ela-migration/module-frameless-tests-final.log`、`module-frameless-native-results.txt`、
`module-frameless-200-results.txt`；截图位于 `build/ela-migration/module-frameless-review/`。

0.31.4 发布构建及 13 项发布回归通过（9.57 秒），另覆盖主窗口的两档缩放和临时编辑器。
记录：`build/ela-migration/release-0.31.4-build.log`、`release-0.31.4-tests.log`。

后续顶栏精简（0.31.5）：独立浮窗只保留资源标题、按需显示的 Fit 与关闭；移除应用图标、
专用拖拽手柄、Pin、完整视图及最小化／最大化按钮。标题文字与空白区拖动统一处理移动和停靠，
双击最大化／还原；边缘缩放保留。Ela 第 22 份补丁提供原生窗口移动通知，资源控制器继续负责
落点、内容所有权和布局保存；侧栏、底栏插入线及取消恢复共用原停靠逻辑。
原生移动循环依据 [Windows 移动结束消息](https://learn.microsoft.com/en-us/windows/win32/winmsg/wm-exitsizemove)
排队完成停靠，避免在系统移动过程中搬移内容。

本次 Release 构建与 10 项针对性 CTest 通过（3.60 秒），覆盖窗口标题、双击还原、窄窗与
100%／125%／150%／200% 布局、拖回侧栏／底栏、取消与能力撤销、内容身份、文档标签及临时编辑器。
隐藏原生 HWND 的移动消息和边缘命中检查通过，窗口未显示、前台焦点未改变；未操控桌面鼠标。
记录：`build/ela-migration/context-minimal-title-build-final.log`、`context-minimal-title-tests.log`、
`context-minimal-title-native-results.txt`；后台截图位于 `context-minimal-title-review/`。

0.31.5 发布构建及 13 项发布回归通过（5.07 秒），另覆盖主窗口两档缩放与 insight provider。
记录：`build/ela-migration/release-0.31.5-build.log`、`release-0.31.5-tests.log`。

0.31.7：浮窗停靠采用覆盖式落点预览，不再为拖动提前展开两个停靠区。释放后沿用同一个内容实例，
先完成目标布局，再由 `ContextDockTransition` 在独立、无焦点且鼠标穿透的临时层内播放 200 ms 过渡。
快照只平移和裁切，新旧内容在末段交接；目标真实控件完成绘制后撤去覆盖层。
窗口缩放、隐藏、再次输入、资源删除和其他面板动画均会结束当前过渡并释放图像。
底部 Context 与 Problems／Activity 改为纵向分行，Context 内仍横向平铺，避免停靠到狭窄角落。
Windows 复用 `NativePanelComposition`，新增不透明度效果组和延迟交接；接口或设备不可用时使用
Qt 栅格过渡，快照超过预算时即时停靠。原生合成支持绑定本进程的透明顶层窗口，依据
[CreateTargetForHwnd 文档](https://learn.microsoft.com/en-us/windows/win32/api/dcomp/nf-dcomp-idcompositiondevice-createtargetforhwnd)。

验证：Release 构建及 19 项相关 CTest 通过（33.85 秒），覆盖四档 DPI、侧栏／底栏插入、
隐藏目标预览不改布局、取消与中断、动画中销毁宿主、快照背景像素、内容实例、光标、滚动和撤销状态。
100%／200% 的后台过渡及落位截图已核对，Windows 隐藏 HWND 的透明合成与延迟不透明度动画检查通过。
这些检查不包含桌面鼠标拖动或可见原生动画逐帧验收。
记录：`build/ela-migration/dock-transfer-build-final.log`、`dock-transfer-tests-final.log`、
`dock-transfer-native-hidden.txt`；截图位于 `dock-transfer-review/`。

0.31.7 发布构建及 20 项发布检查通过（33.77 秒），包括上述回归与版本／文档一致性检查。
记录：`build/ela-migration/release-0.31.7-build.log`、`release-0.31.7-tests.log`。

- 模块节点按文字和层级布局，悬浮高亮；选中分支外的节点透明度降至 0.28。图中保留完整层级。
- 初次显示与 Fit 后随窗口适配；滚轮或拖动后保留手动视角。Fit 在停靠标题条和浮窗操作条中。
- 语义展开忽略的实例由源码结构补充，使用虚线；有定义的不可达分支继续展开，未知定义与循环在边界停止。
- 双击和浏览历史跳转到模块定义；源码跳转失败时不改历史。面板与完整视图保留目标，接收该目标的新语义版本。
- 侧栏纵向、底栏横向平铺，拖动分隔条同时调整相邻两块；不足最小尺寸时使用滚动视口。
- 视图状态升级到 `zeroslack-live-insights-view/v2`，旧 Follow/Pin 字段被忽略。停靠状态升级到 v7，保存区域、高度、宽度、顺序和折叠状态，兼容 v5/v6。

## 扩展 smoke 基线

`gui_smoke_test` 的 977 项检查中存在 20 项失败，集中在标签会话、后台诊断、临时编辑器和目标拾取等既有流程。
已从 `9af95b3557c5140f82a0fd0aed8d4d00581c5d0f` 提取原始测试及头文件，编译后链接正式包 0.30.3 的 DLL 对照：
同样为 977 项、20 项失败。最终代码再次运行耗时 62.70 秒，失败名称与基线完全一致，差异为 0。
未删除这些断言，也不将该 smoke 记录为通过。
本地记录：`build/ela-migration/module-baseline/run.log` 与 `build/ela-migration/module-final-smoke.log`。
