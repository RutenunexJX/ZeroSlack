# ZeroSlack Handoff

ZeroSlack is a lightweight SystemVerilog workspace browser and editor.

```text
Tree-sitter live syntax and editor scope
Slang semantic facts
ProjectModel / DocumentModel / AnalysisScheduler
SemanticIndex / SemanticIndexSnapshot
Query Services and feature services
Thin UI consumers
```

## Hard Rules

- Use Qt 6 + CMake + Ninja only.
- Do not restore qmake, `*.pro`, `*.pri`, `.claude`, SVLexer, the old Tree-sitter symbol parser, the Tree-sitter verify button, regex relationship analysis, or long-lived scattered perflog probes.
- Never discard user changes or use destructive git commands unless explicitly requested.
- Do not touch user dirty RTL fixture files.
- Do not push unless explicitly asked.
- Keep local commits coherent, concise, and architecture-oriented.
- Keep source, tests, UI strings, CMake, and these docs English / ASCII.

## Latest Strategy

- Phase E semantic data model hardening is complete on this branch.
- Phase F0 Legacy Field Retirement is complete on this branch.
- Phase F1-F3 semantic migration is complete on this branch, including record-first relationship/reference/report paths and the post-F3 release gate.
- Phase G Complete Legacy Field Deletion is complete on this branch, including the post-G release gate.
- Phase H Semantic Core Slimdown is complete on this branch: legacy type adapters, redundant APIs, and compatibility logic were removed from semantic core contracts or tightened behind guarded transition boundaries.
- Phase I Semantic Store Native / Collector Native is complete on this branch: the legacy collector/store body has been replaced by semantic-native collection and storage.
- Phase J Test Fixture Native Cleanup is complete: tracked tests use semantic-native fixture builders, `test_sv/syminfo*` and the reverse fixture adapter have been deleted, and the final zero target scans repo source except docs and guard definitions.
- I0 is complete: `legacy_field_policy_guard.ctest` now defines the Phase I zero-legacy target terms and an opt-in `ZEROSLACK_PHASE_I_ZERO_TARGET` final scan for I5.
- G0 is complete: `SemanticRelationshipResult` no longer carries legacy `fromSymbol` / `toSymbol` endpoint payloads; relationship consumers use endpoint records and stable keys.
- G1 is complete: `SemanticDefinitionResult` no longer carries legacy `symbol` payloads; consumers use `symbolRecord` / `symbolStableKey`.
- G2 is complete: `SemanticIndex` / `SemanticIndexSnapshot` no longer expose the retired `SymbolInfo` public APIs such as `getSymbols`, `getSymbolsByType`, `getSymbolByStableKey`, or `findDefinitions`.
- G3 is complete: direct `symbolId`, `symbolType`, `moduleScope`, `dataType`, and `sym_type_e` product-facing use has been removed or isolated to collector/import, taxonomy, completion compatibility, snapshot-local, and guarded adapter boundaries.
- The Phase G release gate passed locally with full Ninja and full CTest.
- The Phase H release gate passed locally with full Ninja, full CTest, `legacy_field_policy_guard`, static legacy API scans, docs consistency review, and forbidden-file regression checks.
- Completion compatibility, snapshot/store `SymbolInfo` carriers, feature-service internal helpers, `syminfo` legacy queries, and collector/import adapter surfaces were tightened during Phase H and later retired by Phase I/J cleanup.
- Phase I progress tracking used per-subphase accounting. Current subphase: I5 complete.
- I1 first block is complete: Slang collection now exposes semantic-native record APIs and `SymbolAnalyzer` consumes `extractSymbolRecords` / `extractWorkspaceSymbolRecords` directly instead of converting collector symbols in the analyzer layer.
- I1 second block is complete: `slangsymbolcollector` now builds `SemanticSymbolRecord` as the primary collector output.
- I1 is complete: `collectSymbols`, `extractSymbols`, and `extractWorkspaceSymbols` are deleted from the tracked collector/manager/test source, leaving Slang symbol extraction record-native.
- I2 first block is complete: `SemanticIndex` now owns a semantic-native record/content store with local handles and stable-key indexes.
- I2 second block is complete: native file content state now drives `contentAffectsSymbols`, and native records/content override older snapshot entries after file replacement.
- I2 third block is complete: native file coverage now prevents stale snapshot or `sym_list` fallback records from resurfacing after a file is replaced with zero semantic symbols.
- I2 fourth block is complete: `SemanticIndex::refreshStructTypedefEnumForFile` is native-only, and the redundant `sym_list` refresh API is deleted and guarded against returning.
- I2 fifth block is complete: `SemanticIndex::getSymbolRecords` no longer reads legacy `sym_list` records through the adapter; record queries merge only native store and snapshot data.
- I2 sixth block is complete: `SemanticIndex` cached-content and content-change queries no longer fall back to `sym_list`; missing native state conservatively requests analysis.
- I2 is complete: `SemanticIndex` no longer reads legacy `sym_list` records through the adapter, and the former I3 write mirror has been removed.
- I3 is complete: scope rebuild, module containment, relationship containment, and module lookup have moved away from the legacy relationship mirror.
- I3 first block is complete: `SmartRelationshipBuilder` consumes semantic records through a `SemanticIndex` record provider instead of reading records back from `sym_list` or the collector adapter.
- I3 second block is complete: `SymbolRelationshipEngine` file rebuild paths consume semantic records through a provider and no longer expose the legacy `symbols()` DB-to-record accessor.
- I3 third block is complete: `sym_list` relationship rebuild and scope refresh now forward containment work to the semantic-record relationship engine instead of adding CONTAINS edges from legacy fields.
- I3 fourth block is complete: legacy `sym_list` current-module lookup and module-scope auto-inference are deleted; module lookup stays on semantic-record `SemanticIndex` paths.
- I3 fifth block is complete: `SymbolRelationshipEngine` no longer accepts, stores, or exposes a `sym_list` database pointer; relationship engine files are guarded against reintroducing `sym_list`.
- I3 sixth block is complete: `SemanticIndex` now owns the attached relationship engine pointer for relationship queries, completion relationship facts, and snapshot publication instead of fetching it from `sym_list`.
- I3 seventh block is complete: `sym_list` no longer stores, exposes, or forwards a relationship engine; `SemanticIndex` directly rebuilds native relationship facts when records change or an engine is attached.
- I3 eighth block is complete: `SemanticIndex` no longer mirrors semantic records back into `sym_list`, and the reverse relationship/scope mirror conversion is deleted and guarded against returning.
- I4 is complete: remaining legacy carrier APIs and compatibility taxonomy surface have been deleted or isolated now that semantic store, scope, and relationship paths no longer depend on them.
- I4 first block is complete: the unused `semanticcollectoradapter` source/header have been removed from CMake and guarded as forbidden legacy collector adapter files.
- I4 second block is complete: `SemanticIndex` no longer exposes or stores a legacy `sym_list` database injection/access API.
- I4 third block is complete: dead `sym_list` symbol database mutator/accessor/index/scope-tree storage has been deleted, including `syminfoindex.cpp`, `syminfoscope.cpp`, and `scope_tree.h`.
- I4 fourth block is complete: native taxonomy metadata checks no longer route through the retired private raw-kind bridge.
- I4 fifth block is complete: unused legacy metadata write-back helpers have been deleted from the taxonomy compatibility surface.
- I4 sixth block is complete: legacy `SymbolInfo` owner/visibility taxonomy overloads have been removed from the public compatibility header; callers verify the semantic metadata contract instead.
- I4 seventh block is complete: legacy `SymbolInfo` interface helper and declaration-helper overloads have been removed from the public taxonomy compatibility header.
- I4 eighth block is complete: legacy `SymbolInfo` scope/visibility helper APIs have been deleted; definition visibility callers use the native semantic metadata overload.
- I4 ninth block is complete: legacy taxonomy no longer exposes `packageScopeNames(QList<SymbolInfo>)`; tests compute package scopes locally before building semantic records.
- I4 tenth block is complete: legacy completion type matching compatibility APIs have been deleted in favor of `SemanticCompletionKind` metadata matching.
- I4 eleventh block is complete: legacy outline grouping compatibility APIs have been deleted; outline checks now use native semantic metadata and raw collector kind directly.
- I4 twelfth block is complete: legacy raw collector kind round-trip helpers have been removed from the public taxonomy compatibility header.
- I4 thirteenth block is complete: tracked tests no longer include the removed `semanticcollectoradapter` header; legacy fixture-to-record conversion is isolated in a test fixture helper and guarded against adapter include regressions.
- I4 fourteenth block is complete: direct `symboltaxonomylegacy.h` includes in tracked tests are isolated to the legacy fixture conversion helper and guarded against spreading back into test bodies.
- I4 fifteenth block is complete: global `symboltaxonomylegacy.h` includes are guarded to remain isolated to `symboltaxonomy.cpp` and the test fixture conversion helper.
- I5 is complete: the Phase I release gate passed with full Ninja, full CTest, normal `legacy_field_policy_guard.ctest`, opt-in `ZEROSLACK_PHASE_I_ZERO_TARGET`, static core legacy scans, and `git diff --check`.
- I5 first block is complete: lowercase `rawCollectorKind` / `requestedRawCollectorKind` field and helper names were renamed to semantic-native `collectorKind` / `CollectorKind` naming, and the guard continues to reserve the raw names as retired compatibility terms.
- I5 second block is complete: `symboltaxonomylegacy.h` and the production `sym_type_e` taxonomy overloads were deleted; tracked tests moved through a temporary fixture-local adapter before Phase J removed it.
- I5 third block is complete: root/core `syminfo*` carrier files moved into `test_sv` fixture scope, core CMake stopped building them, and Phase J later deleted the fixture carrier entirely.
- I5 fourth block is complete: stale core `syminfo` includes and `SymbolInfo` naming were removed, tracked tests were migrated to fixture-local/native semantic helpers, GUI smoke fixtures synchronize native records before snapshot-only checks, and the final Phase I release gate passed.
- J0 is complete: `legacy_field_policy_guard.ctest` defines the repo-wide fixture cleanup allowlist and guards new `sym_list` / `syminfo` legacy terms outside the current cleanup scope.
- J1 is complete: semantic-native test record builders cover metadata, owners, type info, local handles, stable keys, and relationship endpoints.
- J2 is complete: completion and jump tests use semantic-native records instead of hand-authored `sym_list::SymbolInfo` fixture data.
- J3 is complete: relationship and GUI smoke tests use semantic-native records and no longer include or populate the fixture-only `sym_list` carrier.
- J4 is complete: `test_sv/syminfo*` files and the reverse fixture adapter in `semantic_fixture_records.h` have been deleted, CMake no longer builds fixture carrier sources, and full Ninja/full CTest/guard checks passed.
- J5 is complete: the final zero target now scans first-party repo source, including tracked test fixtures, while excluding docs and guard definitions; full Ninja, full CTest, normal guard, final zero-target guard, static scans, and `git diff --check` passed.
- Phase J progress tracking used per-subphase accounting. Current subphase: J5 complete.
- Post-J editor navigation, responsiveness, and completion trigger fixes are committed through `d7c72d5`: completion popup sizing now uses real item text width, interface definitions preserve cross-file header locations, instance named port clicks jump to child port declarations while actual signal clicks stay local, Navigation avoids root-cause synchronous rebuilds by caching hierarchy inputs and batching tree population without timer-delay workarounds, normal completion triggers from identifier prefixes or strong contexts without space/semicolon dependence, semantic command completion uses `;cmd` plus Space, code templates use `;;cmd` plus Space, `;:` is reserved for future expansion, `;?` / `;;?` show scoped inline help, and Ctrl+Space opens the ZeroSlack Global Control overlay from editor or non-editor focus.
- Phase K Follow-up Editor Structure Features is complete. Current subphase: K3 Fold Block Shelf complete, 0% remaining.
- Phase K delivered K1 Design Hierarchy with right-click Design Top and Files dimming, K2 Tree-sitter-backed Code Folding plus Custom Folding through the Global Control `fd` command, and K3 Fold Block Shelf through the Global Control `fds` command.
- Latest Fold Region / Fold Shelf polish adds visible status-bar mode chips, live Fold Region range preview with gutter badges, Fold Shelf dock/list active styling, and Fold Shelf text-input blocking while drag mode is active.
- Phase K verification passed with focused Ninja targets, `completion_test` with 315 checks, `gui_smoke_test` with 331 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, `git diff --check`, normal `legacy_field_policy_guard`, and opt-in `ZEROSLACK_PHASE_J_ZERO_TARGET`. The latest workspace/UI/Fold/Global Control polish additionally passed focused `completion_test` with 321 checks and `gui_smoke_test` with 338 checks.
- Phase L Regex Logic Native Cleanup is complete. Current subphase: L6 Final Regex Zero Target complete, 0% remaining.
- Phase L removed regex-driven logic from first-party production semantic/editor feature paths, replacing it with deterministic SV token helpers while preserving semantic record and `SemanticIndex` data flow.
- Phase L verification passed with full CMake/Ninja build, full CTest 7/7, focused `completion_test` with 321 checks, `jump_test` with 115 checks, `gui_smoke_test` with 331 checks, normal `legacy_field_policy_guard`, opt-in `ZEROSLACK_PHASE_L_ZERO_TARGET`, static production regex API scan, and `git diff --check`.
- Post-L Signal Kernel Graph is implemented: selected signals expose a right-click `Show Signal Kernel Graph` action, the graph renders drivers on the left, the kernel signal in the center, consumers on the right, cross-module groups with module wrappers, hover enlargement plus precise code preview, Ctrl+left-click rebase, and double-click navigation to evidence or declaration.
- Relationship evidence now carries a precise file/line/column range from Slang extraction through `SymbolRelationshipEngine`, `SemanticRelationshipResult`, `SignalJourneyService`, and `SignalKernelGraphService` so graph hover and navigation use assignment/read/connection evidence instead of nearest-symbol guesses.
- Post-L inline declaration template polish is implemented in the current worktree: semantic `;cmd` completion covers `;l`, `;w`, `;r`, `;p`, and `;lp`; `;;l`, `;;w`, and `;;r` share the same no-regex dimension parser; `;;p` and `;;lp` generate scalar or unpacked parameter arrays, support command-local type suggestions after `-`, and leave the cursor at the value or array assignment pattern body.
- Editor range helpers now pair `[` as `[]`, expand bracket contents with Tab (`[8]` to `[7:0]`, `[P_W]` to `[P_W - 1:0]`), and let Ctrl+left-click select a range body so Up/Down adjusts the left bound while Shift+Up/Down adjusts the right bound.
- Post-L Ghost Inline Values is implemented in the current worktree: `GhostAnnotationService` produces transparent editor overlay annotations for formal port details, parameter/localparam literal values, parameter overrides, parameter/macro-derived signal widths, nonzero numeric ranges, array extents, enum values, part-select widths, generate loop counts, and concatenation widths. Numeric base conversion is hover-only as `(D)... (B)... (H)...`, skips comments and strings, and updates from the current buffer like the overlay annotations. The implementation uses semantic records plus deterministic token scans, not regex, and the editor renders the output without changing source text.
- Ghost Inline Values verification passed with focused `completion_test` at 370 checks and `gui_smoke_test` at 351 checks against `test_sv/new` plus `test_sv/test_symbols.sv`.
- Formatter initial MVP is implemented in the current worktree: `FormatterService` established a conservative indent-only SystemVerilog formatter, changed only leading whitespace in that first milestone, ignored strings/comments while counting block structure, preserved preprocessor directive indentation, and exposed `Format Document` from the editor context menu as one undoable edit block.
- Formatter MVP verification passed with focused `completion_test` at 378 checks and `gui_smoke_test` at 354 checks against `test_sv/new` plus `test_sv/test_symbols.sv`.
- Formatter Declaration Alignment MVP is implemented in the current worktree: the same `Format Document` path now also aligns consecutive simple signal declaration blocks and parameter/localparam assignment columns after indentation, while skipping preprocessor lines, comments, multi-declaration lines, and complex non-declaration statements.
- Formatter Declaration Alignment MVP verification passed with focused `completion_test` at 404 checks and `gui_smoke_test` at 361 checks against `test_sv/new` plus `test_sv/test_symbols.sv`.
- Formatter Port List Alignment MVP is implemented in the current worktree: `FormatterService` now aligns contiguous simple ANSI `input` / `output` / `inout` port-list lines by direction, type/range prefix, and port name after indentation, while skipping comments, preprocessor lines, multi-port lines, default-value ports, inline closing forms, and complex port declarations.
- Formatter Port List Alignment MVP verification passed with focused `completion_test` at 414 checks, `gui_smoke_test` at 365 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, full default CMake build, and full `ctest --output-on-failure` 7/7.
- Formatter Instance Map Alignment MVP is implemented in the current worktree: `FormatterService` now aligns contiguous simple named association lines such as `.PARAM(value)` and `.port(signal)` by association name before the opening parenthesis, covering both parameter override maps and instance port maps while preserving expression text and skipping comments, preprocessor lines, inline semicolon forms, and complex non-single-line associations.
- Formatter Instance Map Alignment MVP verification passed with focused `completion_test` at 417 checks, `gui_smoke_test` at 365 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, full default CMake build, and full `ctest --output-on-failure` 7/7.
- Formatter Trailing Comment Alignment MVP is implemented in the current worktree: `FormatterService` now lets simple declaration blocks, ANSI port-list blocks, and named instance-map blocks keep trailing `//` comments while still participating in alignment; comments are reattached at an aligned comment column, while block comments, preprocessor lines, and complex skipped forms remain untouched.
- Formatter Trailing Comment Alignment MVP verification passed with focused `completion_test` at 420 checks, `gui_smoke_test` at 365 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, full default CMake build, and full `ctest --output-on-failure` 7/7.
- Formatter Format Selection MVP is implemented in the current worktree: `FormatterService::formatSelection` formats selected full-line ranges by removing and restoring the common base indentation, and the editor context menu now exposes `Format Selection` when text is selected so selection formatting remains one undoable edit without pulling nested code back to column 0.
- Formatter Format Selection MVP verification passed with focused `completion_test` at 423 checks, `gui_smoke_test` at 368 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, full default CMake build, and full `ctest --output-on-failure` 7/7.
- Formatter Profile Controls MVP is implemented in the current worktree: `FormatterService` now exposes explicit `Structured` and `Indent Only` profiles, editor instances remember the selected formatter profile, the editor context menu adds a `Formatter Profile` selector, and formatting status messages name the active profile.
- Formatter Profile Controls MVP verification passed with focused `completion_test` at 435 checks, `gui_smoke_test` at 377 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Formatter Profile Persistence MVP is implemented in the current worktree: `FormatterSettings` persists the selected formatter profile through `QSettings`, `EditorCoordinator` applies it to open and newly-created editors, and editor profile changes write back to settings so the next editor/session keeps the user's choice.
- Formatter Profile Persistence MVP verification passed with focused `completion_test` at 435 checks, `gui_smoke_test` at 386 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Formatter Format-on-Save MVP is implemented in the current worktree: `FormatterSettings` now persists an opt-in `Format On Save` switch, the editor context menu exposes it beside formatter profiles, `EditorCoordinator` distributes it to open and new editors, and `TabSaveController` formats the editor buffer with the active profile immediately before writing a saved file.
- Formatter Format-on-Save MVP verification passed with focused `completion_test` at 435 checks, `gui_smoke_test` at 396 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Formatter Case Item Alignment MVP is implemented in the current worktree: `FormatterService` now aligns simple same-level `case` / `casex` / `casez` item labels by colon in the Structured profile, preserves trailing `//` comments at a stable comment column, skips preprocessor/block-comment/uncertain lines, and leaves the behavior disabled in `Indent Only`.
- Formatter Case Item Alignment MVP verification passed with focused `completion_test` at 446 checks, `gui_smoke_test` at 403 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Formatter Enum Item Alignment MVP is implemented in the current worktree: `FormatterService` now aligns simple multi-line enum member value columns inside `enum ... { ... }` blocks in the Structured profile, preserves trailing commas and `//` comments, skips complex/uncertain lines, and leaves `Indent Only` unchanged.
- Formatter Enum Item Alignment MVP verification passed with focused `completion_test` at 506 checks, `gui_smoke_test` at 416 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Formatter Assignment Alignment MVP is implemented in the current worktree: `FormatterService` now aligns consecutive simple continuous/procedural assignment statements by `=` or `<=` in the Structured profile, preserves trailing `//` comments, skips declarations, case-item suffixes, preprocessor/block-comment/uncertain lines, and leaves the behavior disabled in `Indent Only`.
- Formatter Assignment Alignment MVP verification passed with focused `completion_test` at 451 checks, `gui_smoke_test` at 405 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Formatter Continuation Indent MVP is implemented in the current worktree: `FormatterService` now tracks unterminated `(` / `[` / `{` delimiter continuations and indents continuation lines one extra level while keeping module/interface/function/task headers at the existing port-list depth. It remains an indentation-only behavior, so `Indent Only` keeps it enabled while alignment passes stay disabled.
- Formatter Continuation Indent MVP verification passed with focused `completion_test` at 459 checks, `gui_smoke_test` at 408 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Formatter Continuation Operator Alignment MVP is implemented in the current worktree: `FormatterService` now aligns leading binary-operator continuation lines in the Structured profile to the right-hand-side start column of the preceding multi-line assignment, while `Indent Only` keeps that structural alignment disabled.
- Formatter Continuation Operator Alignment MVP verification passed with focused `completion_test` at 485 checks, `gui_smoke_test` at 412 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Formatter Ternary Continuation Alignment MVP is implemented in the current worktree: the same Structured continuation-alignment pass now covers leading `?` / `:` ternary branches, aligning them to the preceding multi-line assignment RHS start column while leaving `Indent Only` unchanged.
- Formatter Ternary Continuation Alignment MVP verification passed with focused `completion_test` at 489 checks, `gui_smoke_test` at 412 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Formatter Declaration Array Dimension Alignment MVP is implemented in the current worktree: `FormatterService` now aligns unpacked array dimension suffixes in consecutive simple signal and parameter/localparam declaration blocks, preserving assignment/comment alignment and leaving `Indent Only` unchanged.
- Formatter Declaration Array Dimension Alignment MVP verification passed with focused `completion_test` at 495 checks, `gui_smoke_test` at 414 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Formatter Parameter Port List Alignment MVP is implemented in the current worktree: `FormatterService` now aligns simple `parameter` / `localparam` rows inside module `#(...)` parameter port lists, preserving comma and final no-terminator endings while keeping normal signal/port declaration alignment restricted to semicolon-terminated declarations.
- Formatter Parameter Port List Alignment MVP verification passed with focused `completion_test` at 502 checks, `gui_smoke_test` at 416 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Formatter Single Statement Body Indent MVP is implemented in the current worktree: `FormatterService` now indents simple single-statement bodies after single-line `if` / `else if` / `else` / `for` / `foreach` / `while` / `repeat` headers that do not use `begin`, changing only leading whitespace and keeping `Indent Only` behavior enabled.
- Formatter Single Statement Body Indent MVP verification passed with focused `completion_test` at 513 checks, `gui_smoke_test` at 417 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Wave Preview Data MVP is implemented in the current worktree: `WavePreviewService` derives a lightweight waveform sketch report from current document text, grouping continuous assignments and multiple procedural `always` blocks into per-signal lanes with assignment kind, trigger text, source signals, line/column evidence, and a simple cycle offset for clocked blocks. This is intentionally a code-understanding data layer, not an engineering simulator.
- Wave Preview Data MVP verification passed with focused `completion_test` at 392 checks and `gui_smoke_test` at 354 checks against `test_sv/new` plus `test_sv/test_symbols.sv`.
- Wave Preview Preview-Panel MVP is implemented in the current worktree: `WavePreviewPanelCoordinator` renders the service report in a View-menu dock with per-signal lanes, assignment events, timing hints, source signals, and source locations. The panel refreshes from the active dirty editor buffer while visible, refreshes on open/reset, and keeps extraction outside UI paint code.
- Wave Preview Preview-Panel MVP verification passed with focused `completion_test` at 392 checks and `gui_smoke_test` at 361 checks against `test_sv/new` plus `test_sv/test_symbols.sv`.
- Wave Preview Canvas MVP is implemented in the current worktree: the Wave Preview dock now includes a lightweight graphical lane canvas above the event tree, drawing signal rows, `t+N` timing guides, and colored assignment blocks from the same `WavePreviewReport` without simulating RTL or deriving semantics in paint code.
- Wave Preview Canvas MVP verification passed with focused `completion_test` at 404 checks, `gui_smoke_test` at 363 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, full default CMake build, and full `ctest --output-on-failure` 7/7; the GUI smoke renders the canvas offscreen and checks that it is not a blank single-color widget.
- Wave Preview Guard Labels MVP is implemented in the current worktree: `WavePreviewService` annotates assignment events with nearby `if` / `else if` and `case` / `default` guard labels, the event tree exposes a Guard column, and the canvas renders guarded assignment blocks from the report without evaluating branch truth.
- Wave Preview Guard Labels MVP verification passed with focused `completion_test` at 408 checks, `gui_smoke_test` at 364 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, full default CMake build, and full `ctest --output-on-failure` 7/7.
- Wave Preview Clock/Reset Groups MVP is implemented in the current worktree: `WavePreviewService` extracts edge-triggered clock/reset signals from `always` event controls, groups blocks by shared clock/reset domains, and the Wave Preview dock shows a Clock/Reset column plus a Clock/Reset Groups section without turning the preview into a simulator.
- Wave Preview Clock/Reset Groups MVP verification passed with focused `completion_test` at 411 checks, `gui_smoke_test` at 365 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, full default CMake build, and full `ctest --output-on-failure` 7/7.
- Wave Preview Queued Refresh MVP is implemented in the current worktree: `WavePreviewPanelCoordinator` now coalesces large-buffer refresh requests with a short single-shot timer, keeps only the latest pending document text, preserves immediate refresh for normal-sized buffers, and cancels pending Wave Preview work when the dock is hidden.
- Wave Preview Queued Refresh MVP verification passed with focused `completion_test` at 430 checks, `large_file_perf_test` at 14 checks against `test_sv/new`, `gui_smoke_test` at 372 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Wave Preview Hover Details MVP is implemented in the current worktree: Wave Preview event rows and canvas assignment blocks now share a source/target detail tooltip with target, expression, sources, kind, block, timing, trigger, clock/reset, guard, and line/column evidence, while lane rows summarize signal-level event/source context.
- Wave Preview Hover Details MVP verification passed with focused `completion_test` at 430 checks, `large_file_perf_test` at 14 checks against `test_sv/new`, `gui_smoke_test` at 374 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Wave Preview Clock/Reset Polarity Hints MVP is implemented in the current worktree: `WavePreviewService` now carries edge-aware clock/reset event-control hints (`posedge` / `negedge`) in block and clock/reset group reports, and `WavePreviewPanelCoordinator` shows those hints in Clock/Reset columns and hover details while preserving the existing clock/reset signal lists.
- Wave Preview Clock/Reset Polarity Hints MVP verification passed with focused `completion_test` at 432 checks, `large_file_perf_test` at 14 checks against `test_sv/new`, `gui_smoke_test` at 374 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Wave Preview Declaration Context MVP is implemented in the current worktree: `WavePreviewService` now attaches current-document signal declaration context to waveform lanes, including port direction, local/internal role, type/range text, declaration text, and declaration location; `WavePreviewPanelCoordinator` renders that context in the tree and shared hover details for targets and sources.
- Wave Preview Declaration Context MVP verification passed with focused `completion_test` at 443 checks, `gui_smoke_test` at 403 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Wave Preview Lane Summary MVP is implemented in the current worktree: `WavePreviewService` now computes per-lane summaries with event count, unique source count, block count, maximum cycle offset, and continuous/combinational/sequential activity flags; `WavePreviewPanelCoordinator` renders those summaries in lane rows, lane hover details, the top summary, and compact canvas lane labels for dense reports.
- Wave Preview Lane Summary MVP verification passed with focused `completion_test` at 453 checks, `gui_smoke_test` at 407 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Wave Preview Loop Guard Labels MVP is implemented in the current worktree: `WavePreviewService` now labels assignments nested under `for`, `foreach`, `while`, and `repeat` statements with the local loop/repeat header, and the existing Guard column, tooltips, and canvas labels render those labels without evaluating loop counts or simulating values.
- Wave Preview Loop Guard Labels MVP verification passed with focused `completion_test` at 456 checks, `gui_smoke_test` at 408 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Wave Preview Ternary Guard Hints MVP is implemented in the current worktree: `WavePreviewService` now labels assignments whose right-hand expression contains a top-level ternary with a `?: <condition>` guard hint, combining that hint with enclosing branch/loop/case guards for continuous and procedural assignments while still avoiding value simulation.
- Wave Preview Ternary Guard Hints MVP verification passed with focused `completion_test` at 491 checks and `gui_smoke_test` at 414 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Wave Preview Semantic Context MVP is implemented in the current worktree: `WavePreviewQuery` can now carry the current semantic snapshot, `WavePreviewService` enriches missing target/source signal context from existing semantic records without scanning the workspace or simulating values, and `WavePreviewPanelCoordinator` passes the published `SemanticIndex` snapshot so tree rows and hover details can show cross-file/source context when available.
- Wave Preview Semantic Context MVP verification passed with focused `completion_test` at 481 checks and `gui_smoke_test` at 412 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Wave Preview Canvas Navigation MVP is implemented in the current worktree: Wave Preview canvas assignment blocks now use the same report-owned line/column evidence as tree rows, show a pointing cursor on hover, and double-click navigate to the assignment location through `WavePreviewPanelCoordinator` without parsing RTL in canvas code.
- Wave Preview Canvas Navigation MVP verification passed with focused `gui_smoke_test` at 416 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, focused `completion_test` at 495 checks, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Wave Preview Canvas Event Selection MVP is implemented in the current worktree: single-clicking a Wave Preview canvas assignment block now selects it, redraws the block with a stronger outline, and pins a concise event summary with target, timing, guard/source context, clock/reset context, and location in the Wave Preview summary line while double-click navigation remains unchanged.
- Wave Preview Canvas Event Selection MVP verification passed with focused `gui_smoke_test` at 417 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Huge Workspace Analysis Plan MVP is implemented in the current worktree: `WorkspaceAnalysisPlanService` builds a workspace symbol-analysis order that prioritizes the active file, dirty open files, and other open files before the remaining project files, while preserving dirty-file protection so stale workspace results cannot overwrite live editor buffers. `AnalysisCoordinator` injects the current-file provider into `AnalysisScheduler`; `WorkspaceSymbolAnalysisController` consumes the plan and keeps `SymbolAnalyzer` as the executor.
- Huge Workspace Analysis Plan MVP verification passed with focused `completion_test` at 397 checks, `large_file_perf_test` at 14 checks, `gui_smoke_test` at 361 checks, full default CMake build, and full `ctest --output-on-failure` 7/7.
- Huge Workspace Expirable Request MVP is implemented in the current worktree: `WorkspaceAnalysisRequestQueue` coalesces workspace-analysis requests while a workspace run is active, keeps only the newest pending project, and lets `SymbolAnalyzer::expireWorkspaceAnalysis()` invalidate stale background results without blocking for a synchronous wait. When an expired or canceled watcher finishes, `WorkspaceSymbolAnalysisController` starts the newest pending request.
- Huge Workspace Expirable Request MVP verification passed with focused `completion_test` at 401 checks, `large_file_perf_test` at 14 checks, `gui_smoke_test` at 361 checks, full default CMake build, and full `ctest --output-on-failure` 7/7.
- Huge Workspace Request Telemetry MVP is implemented in the current worktree: `WorkspaceAnalysisRequestQueue` now reports active and pending request age, pending update pressure, and the last finished active/pending wait telemetry so future Activity/Output reporting and scheduling policy can reason about stale-work pressure without moving timing policy into the controller.
- Huge Workspace Request Telemetry MVP verification passed with focused `completion_test` at 427 checks, `large_file_perf_test` at 14 checks against `test_sv/new`, `gui_smoke_test` at 368 checks, full default CMake build, and full `ctest --output-on-failure` 7/7.
- Huge Workspace Telemetry Activity MVP is implemented in the current worktree: workspace-analysis request telemetry now flows from `WorkspaceSymbolAnalysisController` through `AnalysisScheduler` into `AnalysisProgressCoordinator`, which records queued and resolved request pressure in Activity/Output without moving timing policy out of `WorkspaceAnalysisRequestQueue`.
- Huge Workspace Telemetry Activity MVP verification passed with focused `completion_test` at 427 checks, `large_file_perf_test` at 14 checks against `test_sv/new`, `gui_smoke_test` at 370 checks, full default CMake build, and full `ctest --output-on-failure` 7/7.
- Huge Workspace Foreground Open Documents MVP is implemented in the current worktree: `AnalysisScheduler` now refreshes open-document semantic snapshots synchronously when workspace symbol analysis starts, before the background project batch is launched, while keeping the existing post-workspace refresh for reconciliation.
- Huge Workspace Foreground Open Documents MVP verification passed with focused `completion_test` at 430 checks, `large_file_perf_test` at 14 checks against `test_sv/new`, `gui_smoke_test` at 370 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Huge Workspace Relationship Foreground Refresh MVP is implemented in the current worktree: `AnalysisScheduler` now refreshes open-document semantic snapshots through a shared foreground-refresh helper before launching workspace relationship analysis, so relationship snapshots see live editor buffers before the heavier background relationship pass starts.
- Huge Workspace Relationship Foreground Refresh MVP verification passed with focused `completion_test` at 436 checks against `test_sv/new`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Huge Workspace Diagnostics Refresh Coalescing MVP is implemented in the current worktree: `DiagnosticsRefreshController` still debounces diagnostics panel refreshes, but now preserves the broadest pending scope so a full-workspace refresh request cannot be downgraded by later file-specific refreshes during large analysis bursts.
- Huge Workspace Diagnostics Refresh Coalescing MVP verification passed with focused `completion_test` at 440 checks against `test_sv/new`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Huge Workspace Progress Checkpoint Activity MVP is implemented in the current worktree: `AnalysisProgressCoordinator` now routes workspace symbol progress into status updates and records symbol/relationship Activity checkpoints at 25%, 50%, and 75% for long-running workspace passes, while final 100% visibility stays with the existing completion logs.
- Huge Workspace Progress Checkpoint Activity MVP verification passed with focused `gui_smoke_test` at 398 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Huge Workspace Request Restart Visibility MVP is implemented in the current worktree: when a newer workspace-analysis request replaces stale active work, `AnalysisProgressCoordinator` now emits status-bar visibility and Activity/Output records that the latest request is queued and will restart after the stale run resolves.
- Huge Workspace Request Restart Visibility MVP verification passed with focused `gui_smoke_test` at 400 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Huge Workspace Workspace-Relationship Cancellation Visibility MVP is implemented in the current worktree: `AnalysisProgressCoordinator` now listens for workspace relationship cancellation and records a warning-level Activity/Output event plus status-bar feedback when the workspace relationship pass is cancelled.
- Huge Workspace Workspace-Relationship Cancellation Visibility MVP verification passed with focused `gui_smoke_test` at 402 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Huge Workspace Plan Summary Visibility MVP is implemented in the current worktree: `WorkspaceAnalysisPlan` now records background-file count, `WorkspaceSymbolAnalysisController` emits the prepared plan, `AnalysisScheduler` forwards it, and `AnalysisProgressCoordinator` records an Activity/Output summary plus status-bar message with total, priority, background, open, protected, and current-file plan details.
- Huge Workspace Plan Summary Visibility MVP verification passed with focused `completion_test` at 447 checks and `gui_smoke_test` at 405 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Huge Workspace Priority Bands MVP is implemented in the current worktree: `WorkspaceAnalysisPlan` now records explicit current-file, dirty-open, clean-open, and background file bands; `WorkspaceAnalysisPlanService` owns those bands and the planned order, and `AnalysisProgressCoordinator` renders the band counts in Activity/Output plus the status bar.
- Huge Workspace Priority Bands MVP verification passed with focused `completion_test` at 455 checks and `gui_smoke_test` at 407 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Huge Workspace Band Progress Visibility MVP is implemented in the current worktree: `AnalysisProgressCoordinator` now remembers the prepared workspace priority bands and annotates symbol-analysis status plus Activity/Output checkpoints with the current file's band (`current`, `dirty-open`, `open`, or `background`) without recomputing priority policy.
- Huge Workspace Band Progress Visibility MVP verification passed with focused `completion_test` at 459 checks and `gui_smoke_test` at 410 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Huge Workspace Staged Symbol Publication MVP is implemented in the current worktree: `WorkspaceSymbolAnalysisController` now passes the prepared plan's priority-file count into `SymbolAnalyzer`, and workspace symbol publication emits a diagnostics-preserving snapshot after the priority segment so current/open file symbols become queryable before the background segment completes. Final workspace publication still replaces diagnostics only after the full workspace result is ready.
- Huge Workspace Staged Symbol Publication MVP verification passed with focused `completion_test` at 464 checks and `gui_smoke_test` at 410 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Huge Workspace Band-Staged Symbol Publication MVP is implemented in the current worktree: `WorkspaceAnalysisPlan` now records cumulative publication checkpoints after the current-file, dirty-open, and clean-open priority bands, and `SymbolAnalyzer` publishes diagnostics-preserving symbol snapshots as each checkpoint is crossed before the final background-inclusive publication.
- Huge Workspace Band-Staged Symbol Publication MVP verification passed with focused `completion_test` at 467 checks and `gui_smoke_test` at 410 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Huge Workspace Cancellation Checkpoint MVP is implemented in the current worktree: workspace result assembly now marks canceled runs explicitly, synchronous and asynchronous workspace analysis return through the expired path instead of publishing partial snapshots, and cancellation checks now stop before diagnostics extraction when the cancel provider trips after symbol collection or during file-result assembly.
- Huge Workspace Cancellation Checkpoint MVP verification passed with focused `completion_test` at 472 checks and `gui_smoke_test` at 410 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Huge Workspace Direct Symbol Cancellation MVP is implemented in the current worktree: `AnalysisScheduler` and `WorkspaceSymbolAnalysisController` now expose a non-blocking `cancelWorkspaceAnalysis()` path that clears active/pending workspace symbol requests, preserves discarded-request telemetry, emits workspace-symbol cancellation visibility for Activity/status, and expires the current async workspace symbol run without waiting on the worker thread.
- Huge Workspace Direct Symbol Cancellation MVP verification passed with focused `completion_test` at 474 checks and `gui_smoke_test` at 412 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Huge Workspace Slang Boundary Cancellation MVP is implemented in the current worktree: `SlangManager` workspace symbol and diagnostics extraction now accept the workspace cancel provider, check it across file-list preparation, Slang `fromFiles` boundaries, compilation/diagnostics boundaries, and collector traversal, while canceled symbol collection clears partial records before returning.
- Huge Workspace Slang Boundary Cancellation MVP verification passed with focused `completion_test` at 478 checks and `gui_smoke_test` at 412 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Huge Workspace Relationship Boundary Cancellation MVP is implemented in the current worktree: workspace relationship extraction now accepts the active cancel provider through `SlangManager` and `SmartRelationshipBuilder`, checks it across workspace path preparation, Slang `fromFiles` boundaries, compilation/root boundaries, relationship visitor traversal, and grouped-result assembly, and returns empty per-file relationship buckets instead of leaking partial relationship facts after cancellation.
- Huge Workspace Relationship Boundary Cancellation MVP verification passed with focused `relationship_test` at 772 checks and `gui_smoke_test` at 412 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Huge Workspace Relationship Cancelled Result Contract MVP is implemented in the current worktree: `WorkspaceRelationshipAnalysisResult` now carries a `cancelled` flag, `RelationshipAnalysisWorker` clears partial file relationships and restores the base snapshot when cancellation is observed, `RelationshipAnalysisController` emits the workspace-cancelled signal for cancelled worker results, and `RelationshipResultPublisher` refuses cancelled workspace results.
- Huge Workspace Relationship Cancelled Result Contract MVP verification passed with focused `relationship_test` at 774 checks and `gui_smoke_test` at 412 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Huge Workspace Relationship Result Telemetry MVP is implemented in the current worktree: workspace relationship results now carry processed-file count, relationship count, and elapsed time; `AnalysisProgressCoordinator` logs that telemetry in Activity/Output and status text, and cancelled worker results retain zero/partial metrics without publishing relationship snapshots.
- Huge Workspace Relationship Result Telemetry MVP verification passed with focused `relationship_test` at 777 checks and `gui_smoke_test` at 414 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Huge Workspace File-Band Index MVP is implemented in the current worktree: `WorkspaceAnalysisPlanService` now builds a normalized file-to-band index (`current`, `dirty-open`, `open`, `background`) alongside the planned file order, and `AnalysisProgressCoordinator` consumes that plan-owned index for band labels instead of recomputing priority tiers.
- Huge Workspace File-Band Index MVP verification passed with focused `completion_test` at 498 checks and `gui_smoke_test` at 416 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Huge Workspace Band Summary Metadata MVP is implemented in the current worktree: `WorkspaceAnalysisPlanService` now emits plan-owned band summaries for current, dirty-open, open, and background tiers, including display names, counts, priority flags, and publication checkpoints. `AnalysisProgressCoordinator` renders the plan-owned summary text instead of assembling band-count policy itself.
- Huge Workspace Band Summary Metadata MVP verification passed with focused `completion_test` at 509 checks and `gui_smoke_test` at 416 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- Huge Workspace Tier-Aware Symbol Metadata MVP is implemented in the current worktree: `WorkspaceAnalysisPlan` now exposes file-band metadata per normalized workspace path, `WorkspaceSymbolAnalysisController` passes that plan-owned metadata into workspace symbol analysis, and `SemanticIndex` annotates returned symbol records and published snapshots with current/dirty-open/open/background tier details without recomputing priority policy in query code.
- Huge Workspace Tier-Aware Symbol Metadata MVP verification passed with focused `completion_test` at 516 checks and `gui_smoke_test` at 417 checks against `test_sv/new` plus `test_sv/test_symbols.sv`, changed-file C++ regex API scan, full default CMake build, full `ctest --output-on-failure` 7/7, and `git diff --check`.
- `SemanticIndexSnapshot` is the intended single UI query truth.
- Do not add feature-specific workarounds in UI, scheduler, or analyzer code.
- After Phase J, keep new semantic/test work record-native and keep the repo-source zero target passing.

## Phase K: Follow-up Editor Structure Features

- Status: complete.
- K1 Design Hierarchy view is implemented: Navigation now exposes only Files and Design tabs; Design Top is set from right-click menus on Files or Design Hierarchy nodes; Design Hierarchy shows module-only nodes rendered as `instance_name : module_type`; instance double-clicks navigate to instantiation sites; module-definition navigation is available from context menus; reports are cached by snapshot generation plus selected top; Files view dims entries outside the selected hierarchy without hiding them.
- K2 Code Folding and Custom Folding is implemented: SystemVerilog folding ranges come from Tree-sitter syntax nodes, custom `// fold <alias>` / `// endfold` ranges are parsed from Tree-sitter comment nodes, gutter fold controls collapse/expand ranges by block number, and selecting `fd` from Global Control enters Fold Region Mark Mode to insert custom markers as one undo block with visible mode status, live range preview, gutter start/end badges, and persistent custom-fold background tint. Manually typed marker pairs create fd blocks, broken pairs silently disappear, and editing `// fold <name>` renames the block.
- K3 Fold Block Shelf is implemented: selecting `fds` from Global Control enters Fold Shelf mode and opens the shelf dock with visible mode status and active dock/list styling; custom fold blocks highlight on hover with a clear bottom boundary, move or Ctrl-copy into the shelf through structured MIME data with a compact drag pixmap, shelf mode blocks accidental text input, shelf items can be consumed or Ctrl-copied back into editors at line boundaries, double-click preview is read-only, moved item deletion offers restore/delete/cancel, and shelf operations log to Activity/Output.
- `;:` is intentionally reserved and currently has no completion or execution behavior. Ctrl+Space opens a trimmed Global Control surface with only `ow`, `fd`, and `fds`; `ow` replaces the removed toolbar Open Workspace button and opens the alias-required workspace flow.
- Startup hides Editor Appearance, RTL Insights, Signal Kernel Graph, and Activity until needed; opening or switching a workspace shows Activity/Output. The old main toolbar buttons are removed, selected editor tabs and selected workspace tabs use stronger color contrast, and multiple workspaces can be added as isolated alias tabs that reload the active project model and file watcher when selected.
- K1-K3 must not use regex for structure or custom fold parsing. Custom fold and shelf behavior must reuse Tree-sitter/comment-node parsing and the folding range model.
- Opening files must not rebuild heavy hierarchy reports, run RTL Insights reports, scan workspace files from UI, or run Slang directly from UI.
- Activity/Output should record hierarchy builds, Design Top changes, fold/shelf moves, copies, inserts, restores, and warnings without adding long-lived scattered perflog.

## Phase K Goal Mode

- Track Phase K by feature subphase, not by the whole phase.
- K1, K2, and K3 are each their own 100% unit.
- Current subphase: K3 Fold Block Shelf complete, 0% remaining.
- At the end of every work turn, report the current Phase K subphase and the remaining percentage for that subphase.
- Move to the next subphase only after the current feature is implemented, tested, docs are current, and any coherent local commit requested for that block is complete.
- Phase K is complete only when K1-K3 are implemented and verified, docs are current, `gui_smoke_test` passes for affected UI flows, service tests pass where added, `legacy_field_policy_guard` / final zero-target scans still pass when relevant, and `git diff --check` is clean.

## Phase L: Regex Logic Native Cleanup

- Status: complete.
- Goal achieved: first-party production regex logic was removed from semantic, editor, completion, navigation, scheduling, and RTL Insights feature paths without restoring old parsers or moving policy into UI.
- L0 is complete: the repo-wide regex inventory is documented, normal guard blocks new first-party production regex API use outside the cleanup allowlist, and the optional `ZEROSLACK_PHASE_L_ZERO_TARGET` final scan is defined for L6.
- L1 is complete: low-risk identifier checks, word-boundary keyword checks, and whitespace normalization use deterministic token helpers instead of `QRegularExpression`.
- L2 is complete: completion context extraction no longer uses `QRegularExpression`; struct member, enum, assignment, and module-instantiation context parsing uses deterministic token helpers with focused completion coverage.
- L3 is complete: module range, include, and import context logic no longer uses regex; module-context include/import extraction uses deterministic token parsing while preserving `SemanticIndex` record flow.
- L4 is complete: FSM transition extraction no longer uses raw-file regex scans; case selectors, labels, conditions, assignments, and state references are parsed by deterministic token helpers plus semantic state records.
- L5 is complete: open-document structural scheduling checks no longer use keyword regex and now use deterministic token-boundary matching for the existing lightweight structural keyword policy.
- L6 is complete: the final zero target rejects regex APIs in first-party source and tests outside docs and guard definitions, and verification passed with full build, full CTest, focused tests, guard scans, static scans, and `git diff --check`.
- Phase L must not remove guard regex definitions themselves; those are the enforcement mechanism, not product logic.

## Phase L Goal Mode

- Track Phase L by cleanup subphase, not by the whole phase.
- L0 through L6 are each their own 100% unit.
- Current subphase: L6 Final Regex Zero Target complete, 0% remaining.
- At the end of every work turn, report the current Phase L subphase and the remaining percentage for that subphase.
- Move to the next subphase only after the current regex cleanup block is implemented or documented as complete, verified, docs are current, and any requested coherent local commit is complete.
- Phase L is complete: L0-L6 are complete, docs are current, first-party production code no longer uses regex for semantic/editor feature logic, guard exceptions are limited to docs and guard definitions, focused tests and affected GUI smoke tests pass, full Ninja/full CTest passed, and `git diff --check` is clean.

## Feature Expansion Guardrails

These constraints are mandatory for every new feature.

- New semantic or RTL behavior must flow through `ProjectModel` / `DocumentModel` / `SemanticIndexSnapshot -> Query Service or feature service -> report/model -> UI render`.
- UI code must render service/model output only; it must not run Slang, scan workspace files, own semantic policy, or patch around missing semantic data.
- `AnalysisScheduler` and `SymbolAnalyzer` must stay limited to timing, lifecycle, extraction, publication, and refresh routing; they must not own feature policy.
- New tests and fixtures must use semantic-native records/builders and must not reintroduce `sym_list`, `syminfo`, reverse adapters, legacy taxonomy headers, or legacy field/API names.
- New feature reports must expose stable keys, semantic records or typed report rows, owner/type/source-role metadata, navigation payloads, evidence, confidence, and not-found reasons where relevant.
- Real workspace fixtures, especially `test_sv/new`, must cover behavior that depends on package/import/include/interface/modport/cross-file semantics.
- `legacy_field_policy_guard.ctest` and the final repo-source zero target must keep passing after every feature change.
- Feature work must be rejected or redesigned if it requires qmake/pro/pri, `.claude`, SVLexer, the old Tree-sitter symbol parser, direct UI Slang execution, UI workspace scans, regex relationship analysis, long-lived scattered perflog, or legacy carrier/API resurrection.

## Current Architecture

- `ProjectModel` owns workspace root, SV files, include dirs, defines, top, and ignored paths.
- `DocumentModel` owns open document identity, text snapshots, versions, dirty/saved state, cursor, live module names, and registry-backed text queries.
- `AnalysisScheduler` owns analysis timing, debounce/cancel policy, refresh requests, relationship work, and lifecycle routing; it does not own feature policy.
- `AnalysisProgressCoordinator` owns analysis status text and Activity/Output visibility, including queued/resolved workspace request telemetry, restart visibility for coalesced workspace requests, workspace relationship cancellation feedback, relationship result telemetry, prepared workspace plan summaries, priority-band progress labels, and non-spammy symbol/relationship progress checkpoints.
- `WavePreviewService` owns waveform-sketch extraction and context enrichment. It consumes current document text plus an optional published semantic snapshot, enriches target/source signal context from existing semantic records, and keeps the feature positioned as a code-understanding sketch rather than a simulator or workspace scanner.
- `WorkspaceAnalysisPlanService` owns workspace-analysis ordering policy for huge projects, including explicit current-file, dirty-open, clean-open, and background priority bands. Scheduler/controller code asks it for a planned `ProjectSnapshot`; `SymbolAnalyzer` remains an executor and does not decide active/open/dirty priority.
- `SymbolAnalyzer` consumes plan-derived publication checkpoints from `WorkspaceSymbolAnalysisController` and may publish diagnostics-preserving priority-band snapshots before the final workspace diagnostics replacement; priority policy remains in `WorkspaceAnalysisPlanService`. Canceled workspace results are marked before publication, and Slang workspace extraction receives the same cancel provider at boundary and collector checkpoints so stale partial snapshots and diagnostics are not published after cancellation trips.
- `SmartRelationshipBuilder` passes its cancellation state into workspace relationship extraction, and `SlangManager` checks it at relationship extraction boundaries and visitor traversal points before grouped relationship facts are returned. `WorkspaceRelationshipAnalysisResult` carries processed-file, relationship-count, and elapsed-time telemetry; canceled values are marked, stripped of partial file relationships, routed to the workspace-cancelled signal, and refused by the publisher before any relationship snapshot can be applied.
- `WorkspaceAnalysisRequestQueue` owns active, latest-pending, and cancellation telemetry for workspace-analysis request state. `WorkspaceSymbolAnalysisController` uses it to expire stale active work, restart only the newest pending project, or clear active/pending symbol requests through direct cancellation; `SymbolAnalyzer` remains the asynchronous execution layer.
- `AnalysisScheduler` refreshes open-document semantic snapshots before heavyweight workspace symbol and workspace relationship analysis starts, and again when workspace symbol analysis finishes, keeping dirty editor buffers ahead of background publication while retaining a finish-time reconciliation pass.
- `DiagnosticsRefreshController` owns diagnostics refresh debounce/coalescing. Empty file names represent full-refresh scope and remain dominant while pending, so large workspace analysis bursts do not accidentally narrow an already-requested all-files diagnostics refresh.
- `SemanticIndexSnapshot` should be the single query truth for UI-facing semantic reads.
- Query services and feature services own feature-specific semantic reads and report shaping.
- `SignalKernelGraphService` builds signal-centric driver/kernel/consumer graph models from `SignalJourneyService` reports and relationship evidence; `SignalKernelGraphPanelCoordinator` only renders the model and routes hover/navigation/rebase commands.
- `WavePreviewService` builds lightweight waveform-preview lane reports from current editor text. It owns assignment/block extraction, guard-label extraction, clock/reset grouping, clock/reset edge hints, current-document declaration context, and timing-sketch metadata; UI renders these reports without simulating or scanning workspace files itself.
- `WavePreviewPanelCoordinator` renders `WavePreviewService` reports in a dock with both a lightweight graphical lane canvas and the event tree. It also coalesces large-buffer refresh requests and renders shared source/target hover details, edge-aware clock/reset text, and declaration context before calling the service. MainWindow only routes active editor text, visibility-triggered refresh, and navigation; the panel does not scan workspaces or simulate RTL.
- `GhostAnnotationService` builds passive editor overlay annotations from current document text and `SemanticIndex` records; `MyCodeEditor` only stores and paints the model.
- `FormatterService` owns conservative editor formatting reports, including named `Structured` and `Indent Only` profiles, indentation, simple declaration-block alignment, simple ANSI port-list alignment, simple named instance-map alignment, simple same-level case item label alignment, trailing `//` comment preservation for aligned blocks, and full-line selection formatting with base-indent preservation. `FormatterSettings` persists the selected profile and opt-in format-on-save policy, `EditorCoordinator` applies them to open and new editors, `TabSaveController` invokes save-time formatting when enabled, and editors apply returned text as a single undoable edit while formatter policy stays outside UI code.
- UI code renders service/model output and must not run Slang or scan workspace files directly.
- `MyCodeEditor` owns editor event flow and stable public adapters; focused editor subsystem files own gutter, selection/highlight, geometry, cursor navigation, file identity, syntax state, runtime, completion, hover, and source navigation.
- Inline commands are layered by intent: `CompletionService` owns semantic `;cmd` behavior, `CodeTemplateService` owns `;;cmd` template expansion, and `;:` remains a reserved namespace with no active behavior. Declaration templates must be deterministic token parsers, not regex parsers: `;;l` / `;;w` / `;;r` share signal declaration dimensions, and `;;p` / `;;lp` own parameter type suggestions plus scalar/array value cursor placement.
- Global Control is coordinated through a global event filter plus `GlobalControlService`; Ctrl+Space opens it, and the current command surface is intentionally limited to `ow`, `fd`, and `fds` while broader search remains reserved for future expansion.
