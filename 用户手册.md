# ZeroSlack 用户手册

Current version: `v0.30.1`

经典正式包使用 classic 控件样式。0.29.23 曾把 SuiteUi 设为默认，0.29.24 已退回 classic：
真实桌面使用中判定 SuiteUi 控件路径卡顿不可接受，原因尚未定位。
SuiteUi 仍作为默认关闭的可选后端保留，不随正式包分发。

Ela 迁移分支当前正式版本为 0.30.1，使用独立的 `ZeroSlack-Ela-win64` 包，启动程序为
`ZeroSlack-Ela.exe`。设置中心、欢迎页、导航筛选及上下文面板的基础控件已接入 Ela，
编辑器和专业图功能保持原实现。Ela 包的用户配置与经典版分开；显式打开同一工程时，
工程文件和工程本地设置仍共享。下列 0.29.26–0.29.39 开发阶段的迁移均已纳入本次正式版本；
迁移范围及验证记录见 [Ela 迁移](docs/ela-migration.md)。

0.30.1 将标签拖拽、分屏落点和编辑器浮窗交给 Ela 的本地托管扩展。拖动标签到编辑区边缘可分屏，
拖出窗口可创建独立编辑器浮窗；关闭浮窗将标签送回主窗口，关闭标签仍检查锁定和未保存内容。
上下文浮窗采用 Ela 标题栏和拖拽控制。文档共享、撤销、工作区归属及 TEMP 隔离继续生效。
切换工作区时隐藏其余工作区的编辑器浮窗；TEMP 浮窗保持可见。

0.30.0 增加工作区切换器，显示名称、完整路径和未保存标记；收起侧栏后入口移至标题栏。
切换保留各工程的未保存内容、活动文件、光标和滚动位置。外部文件标记为 TEMP，使用独立的
单文件分析和崩溃恢复，不纳入活动工程的扫描、实例绑定或会话；保存仍写回原文件。
工程／设置图标只在展开的左侧栏内显示。

0.29.26 开发构建继续接入 Ela 树列表和标签栏，覆盖 Files/Design、Problems、Workspace Hub、
分析与搜索结果树，以及文档、导航和命令分类标签。树的层级、语义颜色、筛选与双击导航，
文档标签的未保存提示、排序和分屏仍由原有控制器处理。

0.29.27 开发构建继续迁移专业图工具栏、搜索替换与连接表单、Pinloom 操作区、数字插入
工具和外部修改冲突栏。热点图 Track/Matrix、框图嵌套、状态图及果核图的专用实现保留。
数字输入框会为文字和横向步进按钮预留宽度；单选按钮支持原有互斥选择和禁用状态。
0.29.28 开发构建接入通用文本、数字、候选项输入对话框和对话框操作按钮，以及编辑器
查找替换栏、Peek 与独立浮窗内的控件。确认、取消和未保存文档决策继续由原有 Qt
对话框流程处理。
0.29.29 开发构建接入 Ela 工程菜单、普通右键菜单及列表/表格绘制，覆盖命令结果、
设置分类与快捷键、工作区配置和传播表单。原有助记符、禁用项、勾选、双行文字、
单元格编辑及排序继续生效。原生文件选择器、编辑器右键环形菜单和专业图画布保持原实现。
0.29.31 开发构建的主标题栏采用 ElaAppBar，最小化、最大化／还原和关闭按钮由 Ela 提供。
路径右键复制／定位、侧栏开关及原生缩放和分屏继续可用。关闭仍经过未保存文档确认，
取消后保留窗口与编辑内容。没有替换为 ElaWindow，也没有改变专业图或编辑器。
0.29.32 开发构建将左侧工程／设置／收起入口、右侧功能栏及底栏按钮接入 Ela。
继续使用现有图标；工程入口只显示图标。重复点击收起、右键菜单、悬浮窗批量隐藏／恢复、
Problems 和 Activity 状态数字保留。按钮与数字按内容分配空间，底栏高度适应较大字体。
0.29.33 开发构建接入 Ela 滚动容器及只读文本区，覆盖设置、侧栏堆叠、悬浮详情、
Pinloom 文本／图片预览、Activity、差异及恢复预览。保留各区域字体与颜色、文本选择和
复制、日志追加及滚动位置；滚动条按内容需要显示。代码编辑器继续使用专用实现。
0.29.34 开发构建接入 Ela 文字标签，覆盖通用标题、说明、状态与表单字段名。
保留原有字号、字重、语义颜色、禁用颜色、可选文字、链接和字段快捷键；图片标签与
专用绘制保持原实现。工作区配置页也接入 Ela 滚动容器，列表和表格高度随视口调整。
0.29.35 开发构建将整个左侧栏交给 ElaNavigationBar：工程／设置工具区和 Files／Design
统一装入其内容容器，开合使用 Ela 的显示模式和宽度动画。支持连续反向切换、拖动宽度、
收起按钮与 `Ctrl+1`；窗口停靠位置及工程布局保存继续由现有窗口负责。
0.29.36 开发构建减少侧栏开合时的重复宽度设置；可见代码行不变时复用诊断和语义高亮。
可见文字启用有上限的布局缓存，离开视口后释放，以降低全屏开合时的重复文字计算。
滚动、编辑、窗口高度变化和自动换行导致新代码行进入视口时，仍及时更新显示。
0.29.37 开发构建将侧栏开合的过渡画面与真实布局分离。Windows 使用系统合成层移动
临时画面，编辑器只在开合边界调整尺寸；连续反向切换仍由 Ela 的显示模式控制。
输入、滚轮、窗口尺寸或 DPI 改变会先结束过渡，再交回真实控件。过渡期间文字不缩放，
结束后释放临时画面；图形设备尚未就绪或不支持时使用隔离布局的兼容绘制路径。
0.29.38 开发构建修正侧栏动画收尾：真实布局和焦点先完成重绘，再撤掉系统合成层。
动画画面包含完整的 Ela 侧栏背景和边框，避免结束时切换外观；编辑器仍不逐帧调整尺寸。
0.29.39 开发构建把相同过渡用于右侧 Context 栏、底部工具抽屉及右栏内部区块的折叠。
底栏按钮条保持固定，内容滑入／滑出；右栏区块收起时，其余区块平移并裁切显示。
连续反向切换从当前位置继续。编辑器输入会立即结束过渡，恢复布局、拖动调整尺寸和
删除资源仍立即落实；专用图表、视图状态和编辑功能保留原实现。

0.29.30 开发构建为设置页和 Workspace Hub 增加平滑滚轮操作；反向滚动、键盘定位及
列表刷新可以立即接管。Files/Design 和工作区树增加展开过渡。标签栏滚轮只移动标签
视野，不切换当前文档；触控板直接响应，关闭确认和分屏仍由原有控制器处理。

> 本手册以当前代码中已注册的功能、入口和默认快捷键为准；核对的是源码中的入口与命令声明，
> 未重新进行全部交互的人工验收。尚未真机验证的项集中记录在
> [未验证的手工检查项](docs/unverified-manual-checks.md)。

## 1. 产品定位

ZeroSlack 是面向 SystemVerilog/FPGA 开发的图形化代码编辑器。它将日常文本编辑、工程语义分析、RTL 结构查看和可预览的高风险修改放在同一工作区中。

ZeroSlack 对代码的两类认识需要区分：

| 状态 | 数据来源 | 适用功能 |
| --- | --- | --- |
| 当前编辑缓冲区结构 | Tree-sitter 增量语法树 | 高亮、折叠、结构选择、作用域判断、模板插入位置 |
| 已保存工程语义 | Slang 语义快照 | 类型、位宽、生效值、定义跳转、诊断、层级、图形分析和安全 RTL 修改 |

未保存的代码可以继续编辑和使用语法功能，但语义结果可能显示为 `stale`。`Ctrl+S` 保存文件后会排队执行增量语义分析。

## 2. 快速开始

1. 初始页面点击中间的 Open Project 图标打开工作区，或点击 Recent projects 中带路径的工程；路径过长时居中省略，悬停可查看完整路径。也可使用 `Ctrl+K, Ctrl+O`，或按 `Ctrl+Space` 切到 Commands 后输入 `ow 1`。
2. 在 `Workspace > Workspace Configuration` 中设置 include 目录、defines、忽略目录、文件扩展名和 active top。
3. 在 Navigation 的 `Files` 页打开文件，在 `Design` 页查看模块实例层级。
4. 编辑后使用 `Ctrl+S` 保存并更新语义快照。
5. 将光标放在符号上，可使用 `F12`、右键菜单中的图形分析，或按住 `F24` 进入 Command Mode。

## 3. 主界面

窗口不再显示状态栏。工作区扫描、语义分析和操作消息保存在底栏的 Activity 中。
Activity 按钮上的数字表示尚未查看的重要消息数量；普通进度不计数，也不会自动展开底栏。
打开 Activity 查看后数字清零，Clear 清除日志和计数。面板打开、恢复等操作仍可通过 View 菜单完成。

文件树与右侧 Context 面板各自占据完整高度，底栏仅在编辑器下方展开。Settings 改为中央标签页，右侧保留 Context。
点击右侧功能按钮打开侧栏；再次点击当前功能按钮只折叠对应 section，包括已固定内容，并保留其它 section 和内容状态。拖动侧栏左侧分隔线可扩大图表显示区域，重新打开时保留宽度。支持完整视图的图表可通过完整视图按钮在中央标签页打开。显式 Peek 预览入口仍然保留。
Context 侧栏中的四个 insight section（Signal Kernel Graph、Module Diagram、State Diagram、Signal Hotspot）直接显示该 insight 的真实视图，与 Full View 中是同一套渲染，不再是文字摘要；section 高度用标题条下方的边界拖动调整，侧栏宽度用左侧分隔线调整，两者都随工作区保存。Follow Editor 关闭后该 section 冻结在当前结果，不随光标更新。

新鲜度显示在 section 标题条上（Current / Stale / Update pending 等），鼠标悬停可看摘要与来源说明；正文不再重复 section 名、完整视图入口、单一功能卡片和 workbench 自带的标题与 Detach——完整视图用标题条上的入口，浮层化直接拖 section 出去。

编辑器里右键的 `Signal Kernel Graph`、`Signal Usage Hotspot`、`State Transition Graph`、`Module Block Diagram` 四条命令，都会把对应 kind 的 section 重定向到所选符号并钉住（自动关闭 Follow Editor），section 不存在时创建，侧栏或 rail 被隐藏时一并恢复；它们不再另开中央标签页，需要大图时点 section 标题条上的完整视图按钮。

当前编辑器上下文没有该 section 能用的目标时（例如 Kernel/Hotspot 需要信号、Module/State 需要模块），正文列出它还能到达的目标：没在跟随的编辑器目标，以及该 section 最近看过的三个。点击其中一个会把 section 钉在该目标上并自动关闭 Follow Editor，光标再移动也不会把它换掉；重新勾选 Follow Editor 即恢复跟随。

section 标题条上会显示该 section 当前的目标名（还没有目标时显示 `Pick target`）。点击它进入编辑器里的目标拾取：当前**可见区**内该 insight 能画的目标会闪烁——Kernel/Hotspot/State 闪信号与端口，Module Diagram 闪模块名与实例的类型名。`Tab` / `Shift+Tab` 在候选之间移动，`Enter` 或鼠标左键选定，`Esc` 取消。候选只来自可见区并随滚动跟随，所以目标不在屏幕上时把它滚进来即可；目标在别的文件里时请改用右键命令或完整视图。选定后 section 钉在该目标上（同样自动关闭 Follow Editor）。如果选中的符号实际画不出这张图（例如某个寄存器并不驱动状态机），状态栏会显示具体原因，拾取模式**保持开着**，可以直接接着选下一个。section 被拖成浮层后没有标题条，此时用正文空态里的 `Pick in editor…` 按钮或编辑器右键命令。

insight section 新建时会取一个足以容纳图的初始高度，而不是与其它 section 平分侧栏高度（平分时正文常常只剩工具栏）。这只是初值：照旧可以拖 section 边界把它改小或改大，改后的高度随工作区保存。多个 section 同时展开而侧栏放不下时，未聚焦的 section 会被压到最小高度，聚焦的那个保留大块空间。
`View > Context Sidebar` 或 `Ctrl+2` 显示/隐藏整个 Context 侧栏，隐藏时保留全部 section 及其视图、顺序和宽度，再次触发原样恢复。该项是 Context 的兜底入口：侧栏为空时会打开 rail 第一个功能的默认视图，rail 被隐藏时会一并恢复 rail，因此关掉全部 section 或隐藏 rail 后都能用它回到 Context；只有在没有任何 Context 功能注册时才不可用。

左侧栏将工程操作与文件树放在一起，顶部横向排列工程、设置图标及收起按钮。未打开工程或文件时默认显示精简启动页：左侧只保留工程和设置入口，中间为 Open Project 图标；有最近工程时同时显示名称和路径，路径过长时居中省略，悬停可查看完整路径。工程按钮只显示图标，点击或右键可访问原菜单命令；本文中的 Workspace、View、Tools 路径均指该命令菜单。设置仍在中央标签页打开，再次点击设置图标可关闭当前设置页，不替换侧栏中的文件树。收起按钮或 `Ctrl+1` 让整个侧栏（包括文件树和顶部功能入口）向左滑出，编辑区同步扩展；顶栏左上角保留展开按钮，重新展开时侧栏向右滑入并恢复原宽度。连续触发可反向中断动效；启动页切换与布局恢复仍即时生效。
窗口标题栏固定显示，占用独立高度，不覆盖编辑器或标签页。背景跟随现有主题；标题栏支持窗口拖动和双击最大化，窗口边缘支持缩放。
标题栏上的文件名右键可复制完整路径或在文件管理器中定位，完整路径也显示在悬停提示里；Windows 上支持原生最大化／还原与 `Win+方向键` 贴靠。
界面采用统一圆角控件和可缩放线性图标，工程、上下文与底栏共用图标规范。

非编辑区优先使用 Noto Sans SC 比例字体，缺失时回退到系统字体：页面标题 18 px，面板标题 14 px，正文和操作项 13 px，辅助说明 12 px，计数 11 px。标题、分组与选中标签使用半粗，文件名和普通内容不加粗。侧栏说明与按钮分行，设置页分组保留更清晰的间距；编辑器、代码预览和差异文本沿用原有代码字体设置。
底栏仅常驻 Problems 与 Activity。Search、修改预览、Connections按需出现。修改预览完成或取消后收起。
行号区按“标记列｜行号｜折叠｜代码”排列，宽度按行数位数计算。诊断与 Pinloom 链接共用标记列；同时存在时诊断优先、角标提示链接，点击后明确选择 Pinloom 入口。

### 3.1 工程图标右键命令

| 菜单 | 主要内容 |
| --- | --- |
| `Workspace` | 打开和切换工作区、关闭当前工作区、工程配置、上一条/下一条诊断 |
| `View` | Navigation、Context Sidebar、搜索替换、编辑器分栏、标签页管理与分组、Problems、Activity、底部面板收起、Settings、Reset Panel Layout |
| `Tools` | 用户模板、高风险 RTL Actions、崩溃恢复审查 |

### 3.2 Navigation

Navigation 可通过 `View > Navigation` 或 `Ctrl+1` 显示/隐藏。

| 页面 | 功能 |
| --- | --- |
| `Files` | 按文件名或路径过滤；显示工作区文件和虚拟源文件组；可隐藏与当前设计无关的文件 |
| `Design` | 按实例名或模块名过滤；显示自动推断或用户指定的 top 下的模块实例层级 |

`Files` 右键菜单支持新建文件、新建目录、重命名预览、移至回收站预览、复制完整路径和在文件资源管理器中显示。

`Design` 右键菜单支持跳转到实例化位置、跳转到模块定义和设为 Design Top。Design 树仅作为模块层级浏览器，不展示 interface 节点。

### 3.3 底部面板

底部面板容纳 Problems、Activity/Output、搜索结果和修改预览。Connections 在中央标签页打开，包含
`Instance Pair` 与 `Multi-Signal Propagation` 两个页签。图形分析不在底栏：Signal Kernel Graph、
Module Diagram、State Diagram、Signal Hotspot 都在右侧 Context 侧栏的 section 里，
需要大图时用 section 标题条上的完整视图按钮在中央标签页打开（见 §5.4 与 §16）。

- `Ctrl+J` 收起或恢复底部面板。
- 页面可固定，也可关闭当前页。
- `View > Reset Panel Layout` 恢复默认面板布局。

Settings、搜索替换、修改预览和 Connections 的正文在首次使用时创建；收起或关闭后再次打开会复用原页面。
尚未打开的面板仍保存布局状态，首次打开时恢复页签和选择。设置值在设置页打开前已经生效。
Fold Shelf、自定义 Fold Region、静态 Wave Preview 与 Wave Simulation 已移除；语法折叠和四类专用图表保留。

## 4. 工作区与工程配置

### 4.1 多工作区

ZeroSlack 可同时打开多个工作区，各自保存文件列表、工程配置和会话状态。切换时重建活动工作区的语义上下文。

一个窗口可以保持多个工作区打开，一次显示一个活动工作区。侧栏顶部的名称按钮展开工作区切换器；
侧栏收起后，该入口显示在标题栏，工程和设置图标仍仅在侧栏内显示。

切换器将“已打开”和“最近”分组列出，每项显示工作区名称和完整路径。勾选表示当前工作区，圆点表示
该工作区有未保存文件；右侧叉号关闭对应工作区。`Workspace` 菜单和 Commands 的 `ow` 域仍可使用。

切换时，Files、Design、文件标签和分析上下文跟随活动工作区，并保留各工作区的未保存内容、上次活动文件、
光标与滚动位置。切换不弹保存提示，也不重复创建已经打开的标签。关闭工作区时只处理它自己的未保存文件；
取消会保留整个工作区，确认关闭会一并关闭其锁定标签，不影响其他工作区和 TEMP 文件。

打开或切换工作区时，缓存的文件列表会立即显示，不等待目录扫描；随后 ZeroSlack 在后台把这份缓存与磁盘目录
对账，因此工作区不活动期间新增、删除或改名的文件都会被发现。只有文件集合确实发生变化时才重新发布文件列表，
未变化的文件不会因此重跑语义分析。

### 4.2 Workspace Configuration

| 配置 | 用途 |
| --- | --- |
| `Include Dirs` | 指定 `` `include `` 搜索路径 |
| `Defines` | 为当前 ZeroSlack 分析配置提供宏名和可选值 |
| `Ignored Dirs` | 工作区扫描时排除目录 |
| `File Extensions` | 指定纳入工程的 SystemVerilog/Verilog 文件扩展名 |
| `Top Module / Active Top` | 指定层级分析、实例上下文和高风险跨层修改的顶层 |
| `Virtual Source Groups` | 建立具名、有顺序的虚拟文件组，用于组织和分析文件集 |

可移植的工程配置保存在工作区内的 `.zeroslack/project.json`，路径尽量按工作区相对路径保存。

### 4.3 工作区会话

会话状态包括打开的文件、光标位置、标签页、分栏、面板布局和扫描缓存。它保存在本机应用数据目录，不写入源码树。

| Commands 命令 | 作用 |
| --- | --- |
| `ow s save` | 立即保存当前工作区会话 |
| `ow s restore` | 恢复本机会话；需要时只读导入旧 `.zs` |
| `ow s clean` | 清理当前工作区的本机会话，不删除 `project.json` 或旧 `.zs` |

`.zs` 已是兼容用的只读迁移来源，ZeroSlack 不再将新会话写回 `.zs`。
同一运行期间切换已打开的工作区时使用现有编辑器；不会反复从磁盘会话覆盖正在编辑的视图。

## 5. 文件、标签页与视图

### 5.1 基本文件操作

| 操作 | 默认快捷键 |
| --- | --- |
| 新建文件 | `Ctrl+N` |
| 打开文件 | `Ctrl+O` |
| 保存 | `Ctrl+S` |
| 另存为 | `Ctrl+Shift+S` |
| 重新打开最近关闭的标签页 | `Ctrl+Shift+T` |

当其他应用修改已打开的文件时，ZeroSlack 会跟踪外部变化。无本地未保存修改时可同步内容；存在冲突时需先处理本地内容与磁盘内容的选择。

可用 `Ctrl+O` 或启动参数直接打开任意位置的 `.sv`、`.v` 文件，例如 `ZeroSlack-Ela.exe C:\Temp\scratch.sv`。
打开文件不自动创建工作区。文件不属于任何已打开工作区时，标签显示 `TEMP`；未命名文件也按 TEMP 管理。
TEMP 标签保留在工作区标签之后，以细分隔线区分，切换或关闭工作区后仍然保留。
选中 TEMP 不改变活动工作区，不自动纳入工程扫描、实例绑定或工作区会话。
TEMP 使用单文件语义分析及文件所在目录的包含路径，不继承活动工程的宏和包含目录。
TEMP 不表示内容可以丢弃：`Ctrl+S` 仍保存到原文件，未保存内容的崩溃恢复记录使用独立的本机存储分区。
打开包含该文件的工作区或将它另存到工作区内后，归属与 TEMP 标记会相应更新。

### 5.2 标签页

标签页支持关闭、关闭其他页、关闭右侧页、关闭全部、锁定、复制视图和在 Context Peek 中打开。
标签增多或分栏后，标签保留最小可读宽度，超出的标签通过左右滚动按钮访问，不再持续压缩成省略号。

标签页可按以下方式分组：

- `None`：不按模块或工作区名称排序；TEMP 标签仍独立排列在后部。
- `Module`：按 SystemVerilog 模块分组。
- `Workspace`：按工作区分组。

同一文件的复制视图共享同一份 Document，适合在不同位置同时编辑。

### 5.3 编辑器分栏

`View > Editor Layout` 支持：

- 向左、向右、向上、向下新建分栏。
- 最大化当前分栏。
- 均分分栏尺寸。
- 合并当前分栏。
- 拖放标签页时显示落点预览。


### 5.4 Context Workspace

`Open in Temporary Editor` 会在编辑区右侧的 Context Peek 中打开目标，
不新增分栏，也不改变现有分栏结构。右侧 Context Rail 是统一入口。

- Peek 只保留一个临时内容；再次打开会替换当前 Peek。
- 搜索框可打开文件、module、package 或符号，Back/Forward 在该视图的
  历史位置间移动。
- Pin 按钮把当前内容原样移入右侧 Context Dock，不会重建编辑器或丢失
  光标状态；Dock 支持多个标签页和原生 Qt 拖动、浮动与尺寸调整。
- Unpin 把当前 Dock 标签页原样移回 Peek；`Esc` 或关闭按钮关闭 Peek。
- Dock 的完整视图、Unpin 和关闭当前标签操作位于标签栏上方；关闭前先选中目标标签。未保存文档的标签名称与编辑器一致，完整资源 URI 保留在悬停提示中。
- Context 和专用图表的工具条在宽度不足时整体换行，操作按钮保留完整文字；长标题以尾部省略号显示，并在悬停提示中提供全文。
- 临时编辑器使用共享 Document，与普通标签页共享文本、撤销状态和文件
  身份。关闭 Context View 不会关闭主编辑器中的文件。
- 工作区会恢复已固定内容、标签顺序和活动标签。工作区目录移动后，位于
  工作区内的文件仍按相对路径恢复；编辑区内的临时 overlay 默认不恢复。

Pinloom、RTL Insight 等可分离内容在 Unpin 或显式浮层打开时使用独立原生窗口，
可拖动标题栏、调整窗口大小，使用 Pin 原样移回侧栏，使用原生关闭按钮关闭。
不同资源可以同时浮起；同一资源始终只有一个视图，重新打开会复用已有实例。
临时编辑器仍只保留一个 overlay，可与多个原生浮层共存。
原生浮层资源、位置、大小及屏幕名称随工作区保存；恢复时最多加载 16 个窗口，超出项计入恢复警告。
原屏幕不存在或标题栏不可操作时，窗口回落到主屏可用区域内。

右键点击 Context Rail 图标，可新建全局浮层、创建绑定到当前文档的浮层、
按资源标题聚焦已有实例、修改绑定，或把该功能的全部浮层原样收进侧栏。
如果功能返回的是同一个资源，“新建”会复用已有视图；打开不同资源才能产生多个窗口。
左键优先操作该功能最近聚焦的实例；原生窗口处于活动状态时再点会隐藏它，再点恢复。
侧栏中的实例则只折叠当前 section，不隐藏其它 section；再次点击展开并滚动到该 section。
Rail 末尾有独立的 Hide/Restore 控件，统一隐藏或恢复 overlay 与原生窗口，保留内容和几何；
关闭按钮则释放视图。统一收起状态随工作区保存。

文档绑定使用工作区相对路径：切走时隐藏，切回时原位置显示，内容始终是打开时的资源。
关闭该文件最后一个编辑视图时释放绑定浮层，重新打开该文档后按记录重建；分栏中的同文件视图共享绑定。
最多保存 32 个文档布局，按最近使用淘汰。没有活动文档时绑定入口禁用；绑定到其他文档的实例，
需先切回对应文档才能从列表聚焦，也可通过绑定菜单明确改为全局或绑定到当前文档。
统一收起优先于文档可见性。

Context 侧栏采用可折叠竖栈。每个 section 的标题条包含展开/折叠、预览、完整视图及关闭入口，
可同时显示多个内容区，也可全部折叠为标题条。拖动内容区下方边界调整高度，拖动标题条在栈内重排。
空间不足时先压缩非聚焦 section，仍不足时可以滚动；侧栏本身的关闭/显示开关仍作用于整个侧栏。
顺序、折叠状态和高度随工作区保存。

将可分离 section 的标题条或拖动手柄拖到侧栏外释放，即可原样移为浮层。临时编辑器的拖出手柄禁用，
但预览按钮仍可移回编辑区 overlay。拖回时使用浮层客户区头部的拖动手柄，不能使用原生标题栏：
落在 section 上半部插到它之前，下半部插到它之后，空白区追加；插入线指示落点。
拖动开始时隐藏的侧栏会暂时显示，取消拖动会恢复其原可见性；Pin 和 rail 右键的批量收进入口仍保留。

v5→v6 为单向升级。读取旧工作区时只展开原活动项，其余 section 折叠；旧版本会忽略整个 v6 Context 状态。

独立悬浮窗在支持的 Windows 11 系统上使用 Acrylic 毛玻璃背景。
`Settings > Appearance > Floating background opacity (%)` 仅调整背景色的覆盖浓度，
范围为 60–100%，默认 90%；面板、列表、文本预览及图表空白区域共享背景。
文字、图标、图表节点和连线保持清晰，失焦不会淡化整个窗口；移回侧栏后恢复普通背景。
不支持材质、关闭系统透明效果、开启高对比度或节电模式时，使用主题的实色背景。
该设置为应用级偏好，不写入工作区，也不影响临时编辑器 overlay。

右侧 Context Rail 的 `Pinloom` 入口用于访问 Pinloom 资料库：

- 点击入口会打开或切换 Pinloom Context 内容；在搜索框中输入名称、别名、标签或目标信息。
- 单击结果会解析并显示对应文档、Anchor 或 Saved Clip 的当前内容与元数据。
- `Open` 由 Pinloom 执行原始跳转；`Copy Link` 复制稳定的 `pinloom://` 链接。
- Pin 可将当前 Pinloom 内容固定到 Context Dock。工作区恢复时只保存稳定身份、
  URI 和查询状态，正文仍从 Pinloom 重新读取。
- Pinloom 未运行时 ZeroSlack 会尝试启动它；找不到程序时可在
  `Settings > Integration` 指定 `pinloom_app.exe`。正式 AppPackage 的同级
  `Pinloom` 目录可被自动发现。
- 在编辑器中选中一段代码，右键选择 `Link Pinloom...`，可关联已有条目，
  或使用 `Create Anchor` 将选中内容创建为新的 Pinloom 源码锚点。
- 光标位于已关联的代码范围内时，右键选择 `Open Pinloom Link`，资料正文会直接
  显示在 Context Workspace 侧栏；`Open` 仍用于跳转到 Pinloom 的原始目标。
- ZeroSlack 仅在工作区的 `.zeroslack/pinloom-links.json` 中保存源码范围与稳定
  `pinloom://` URI。工作区移动后按相对路径和文本上下文重新定位，资料正文仍由
  Pinloom 管理。

### 5.5 崩溃恢复

ZeroSlack 保存未保存文档的恢复信息。`Tools > Review Crash Recovery` 可查看、比较、恢复或丢弃候选内容。

## 6. 日常编辑

### 6.1 标准操作

| 操作 | 默认快捷键 | 说明 |
| --- | --- | --- |
| 撤销 / 重做 | `Ctrl+Z` / `Ctrl+Y` | 按编辑事务撤销或重做 |
| 剪切 / 复制 / 粘贴 | `Ctrl+X` / `Ctrl+C` / `Ctrl+V` | 无选区时，剪切和复制作用于当前整行 |
| 全选 | `Ctrl+A` | 选中当前文档 |
| 文件内查找 / 替换 | `Ctrl+F` / `Ctrl+H` | 当前文件文本查找与替换 |
| 复制选区 / 当前行 | `Ctrl+D` | 有选区时复制精确选区并选中新副本；无选区时复制当前逻辑行 |
| 删除整行 | `Ctrl+Shift+D` | 删除光标或选区触及的所有逻辑行 |
| 合并行 | `Ctrl+Shift+J` | 合并选中行，或当前行与下一行 |
| 上移 / 下移行 | `Alt+Up` / `Alt+Down` | 整行移动光标或选区触及的行；支持长按连续移动，光标跟随被移动内容 |
| 注释 / 取消注释 | `Ctrl+/` / `Ctrl+Shift+/` | 作用于当前行或选中行 |
| 缩进 / 减少缩进 | `Ctrl+]` / `Ctrl+[` | 作用于当前行或选中行 |
| 选中整行 | 三击 | 选中光标所在的完整逻辑行；该行后面还有下一行时，换行符一并选入，最后一行则选到文档末尾 |

Tab 默认输入 4 个空格。内联命令候选和 Slot Mode 打开时，Tab 由当前模式处理。

编辑器右键菜单采用直径 168 px 的纯图标主环，分为图表、仿真、Pinloom、重构、文本和临时编辑六组。悬停或点击分类后，在外侧显示圆角矩形图标条；四项以内单行，更多时分两行。图标为 16 px，子项点击区域为 28 px；可执行项使用蓝紫强调色，不可执行项为灰色且不能点击。名称和不可用原因在悬停提示中显示。鼠标经过主环与图标条的间隙不会关闭菜单；中心叉号、Esc 或点击外部可关闭。Tab 切换焦点，Enter 执行焦点按钮。

撤销、重做、剪切、复制、粘贴、全选、跳转定义、跳转行不进入该菜单，原有快捷键等入口保留。`Toggle Selection Case` 可切换选区内字母大小写；`Replace Selection with Spaces` 将普通选区中的非换行字符替换为等量空格，也可将矩形列选区替换为等宽空格，操作均可一次撤销。Replace、注释、取消注释、缩进和减少缩进使用上表快捷键，不在右键菜单重复显示。

光标位于 module 内时，右键菜单的 `Organize Signal Declarations` 会把当前 module 的完整顶层 net/variable 声明集中到安全的声明区。import、typedef、parameter/localparam 等依赖声明仍保留在信号声明之前；原有声明顺序及相邻注释保持不变；always/task/function 等过程块内的局部变量不会移动。当前 module 存在语法错误、条件编译边界或无法确定的结构时，该操作禁用并显示原因。

### 6.2 结构选择与多光标

| 操作 | 默认快捷键 |
| --- | --- |
| 增加下一个同符号出现位置 | Command Mode：`select next occurrence` |
| 选中最近词法作用域内的所有同符号 | `Ctrl+Shift+L` |
| 扩展结构选区 | `Ctrl+W` |
| 移到下一个 / 上一个已选符号 | `Ctrl+E` / `Ctrl+Q` |

Command Mode 中的 `select begin end` 选中最近 `begin-end` 内部的完整行，不包含 `begin` 和 `end` 所在行。

鼠标单击符号仅移动光标；双击符号启用当前文档中的同名符号高亮；在同一位置继续第三次单击会选中完整逻辑行。非末行选区包含换行符，便于整行复制、移动或删除。下一次普通单击会清除同名符号高亮。

### 6.3 列选择和虚拟列

- `Shift+Alt+鼠标拖动` 创建或调整矩形列选区；
  `Shift+Alt+方向键` 可从普通光标直接建立并扩展列选区。
- 列选区已建立后，`Shift+Left` / `Shift+Right` 可直接扩展或收缩列范围，无需继续按住 Alt。
- 列选区支持输入、删除、退格、复制、剪切、粘贴、Tab、Shift+Tab 和方向键。
- 普通单击和方向键不会进入超出行尾的虚拟列。
- `Alt+单击行尾外` 显式进入 Virtual Column；状态提示会标明虚拟列，输入时自动物化所需空格。
- 在行尾外普通单击可保留一个待用虚拟起点，之后的 `Shift+Alt` 列选择可使用该起点。
- `Esc` 退出虚拟列或多光标状态。

### 6.4 Column Number Tool

先建立列选区，再按 `Alt+C` 打开数字序列工具。可配置：

- 十进制、十六进制、八进制或二进制。
- Plain、C-like、SystemVerilog 无位宽或 SystemVerilog 有位宽表示。
- 初始值、步进、重复次数、升序/降序、数字宽度、空格/零填充和十六进制大小写。
- 插入或替换列选区，提交后作为单个撤销事务。窗口会记住上次提交的进制、初始值、步进、重复次数和填充等配置，不显示预览区。

### 6.5 信号复选和赋值队列

1. 在 Command Mode 中执行 `select signals`。
2. 用鼠标左键点击或拖动勾选标识符。
3. 右键结束勾选阶段并打开操作菜单；`Esc` 取消。
4. 选择 `Create Assignment Queue`，生成 `signal <= ;` 队列，所有右值自动进入 Slot Mode。

选中的多个信号也可作为 `Propagate Selected Signals` 的输入。

## 7. 补全、模板和 Slot Mode

### 7.1 统一插入面板

按 `Ctrl+Space` 显式打开统一面板。编辑器获得焦点时默认进入 `Symbols`，其他位置默认进入 `Commands`。

- 直接输入过滤当前类别；上/下方向键移动候选，左/右方向键移动输入光标，Enter 执行。
- Tab / Shift+Tab 在 `Symbols`、`Templates`、`Commands` 之间循环。
- `Esc` 关闭面板，不修改编辑器内容。
- 旧的 `;cmd` / `;;cmd` 不再触发补全；分号按普通源码字符处理。

### 7.2 Symbols

`Symbols` 默认搜索当前编辑位置可见的全部可插入语义符号，包括 port、wire、reg、logic、parameter/localparam、typedef/enum/struct 类型与变量、enum value、instance、task 和 function。候选受当前 module、package、词法作用域和 import 约束，不会把其他模块内部信号混入当前列表。输入内容可持续进行模糊过滤。

类型选择器仅用于收窄默认结果。可在过滤文本开头输入选择器和空格，例如 `w `、`r `、`l `、`p `、`lp `、`t `、`f `、`e `、`s ` 和 `td `。`m <filter>` 专门搜索当前工作区的 module，例如 `m uart`。

### 7.3 Templates

`Templates` 提供以下结构化插入：

- logic、wire、reg、parameter、localparam、assign、always、always_comb、always_ff、module、interface、task、function、enum、packed/unpacked struct 和 define/ifdef 等内置模板。
- 全局和工作区用户模板。
- 基于语义索引生成的完整 module 例化，包括命名 parameter 与 port，并进入 Slot Mode。
- package import；插入位置由 Tree-sitter 语法结构决定。
- header include，以及在当前文件目录优先创建新 header 后插入 include。

模板按当前语法位置过滤；不能可靠确定结构或语义时，操作明确拒绝或采用既有保守模板，不跨作用域猜测。

### 7.4 用户模板

`Tools > User Templates` 可打开全局或工作区模板文件，并手动重载。

- 全局模板文件：`user_templates.json`。
- 工作区模板文件：`.zeroslack/user_templates.json`。
- 工作区模板覆盖同名全局模板。
- 用户模板不能覆盖内置命令，也不能占用 `;;h` 和 `;;pk`。

文件格式为 `{"templates": [...]}`。每条记录使用 `command`、`description`、`body` 或 `insertText`，可选提供相对位置 `slots`。重载时会报告无效命令、冲突和 slot 错误。

### 7.5 Slot Mode

模板、语义 module 例化、`clear right`、赋值队列和 `Edit Instance Slots` 都可进入 Slot Mode。

- 所有 slot 使用可见的周期性提示标记。
- Tab 移到下一个 slot，Shift+Tab 移到上一个 slot。
- 前后移动在 slot 列表中循环。
- `Esc` 完成并退出 Slot Mode。
- `Edit Instance Slots` 可对已有模块实例的参数实参和端口实参重新进入 slot 编辑。

### 7.6 关键字 Ghost Completion

输入 SystemVerilog 关键字前缀时，编辑器可以 ghost 文本显示建议；Tab 接受，`Esc` 或移动光标取消。该功能不恢复旧的无条件通用补全窗。

## 8. Command Mode

Command Mode 用于执行高频编辑和导航 Action，不需要记忆短缩写。

### 8.1 使用方式

1. 按住 `F24`。可在操作系统或键盘重映射中将 CapsLock 等按键映射为 F24。
2. 输入命令全称或有序缩写。例如 `goendm` 可匹配 `go endmodule`，`gm` 可匹配 `go module`。
3. 按 Enter 执行当前命令。
4. 如果 F24 仍被按住，可直接输入下一条命令。
5. 松开 F24 或按 `Esc` 返回编辑器。

普通反引号 `` ` `` 是代码输入，不作为 Command Mode 开关。

### 8.2 编辑与导航命令

| 命令 | 作用 |
| --- | --- |
| `go <number>` | 跳到当前 module 内指定的 1-based 行；`g100`、`go100`、`go 100` 均可解析 |
| `go module` | 打开工作区 module 模糊搜索器 |
| `go package` | 打开工作区 package 模糊搜索器 |
| `go endmodule` | 跳到当前 module 的最后一个 `endmodule` |
| `clear right` | 清空选中行内 assignment 的右值，并将每个右值设为 slot |
| `delete lines` | 删除选区触及的整行 |
| `join lines` | 合并选中行或当前行与下一行 |
| `move lines up` / `move lines down` | 整行移动 |
| `select begin end` | 选中最近 begin-end 块内容 |
| `select signals` | 进入语义信号复选模式 |
| `select next occurrence` | 选中当前符号并增加下一出现位置 |
| `select scope occurrences` | 选中最近词法作用域中的所有同符号 |
| `expand selection` | 逐级扩展到符号层级或外层表达式 |
| `column number` | 打开 Column Number Tool |
| `repeat action` | 重复上一个成功 Action；高风险 Action 会重新进入 dry-run 预览 |
| `help` | 打开 Action Catalog，查看全部 Action、入口和别名 |

Command Mode 还可执行标签页、分栏、面板和高风险 RTL Action。新建、打开、保存、另存为和打开工作区保留菜单/快捷键入口，但不显示在 F24 中。以 `help` 中当前显示的注册项为最终清单。

## 9. Commands

`Ctrl+Space` 后切换到 `Commands`，只显示显式注册到 Global Control 的命令。除工作区命令外，保留 RTL 重命名、实例连接同步、实例对连接、多信号传播、列编号及带行号的跳转。仅供 F24 使用的指令仍只在 F24 Command Mode 显示。工作区命令采用域前缀：

| 命令 | 作用 |
| --- | --- |
| `ow <N>` | 一次选择并打开 N 个工作区 |
| `ow r` | 打开最近工作区列表；可移除单条最近记录，不删除工作区目录 |
| `ow s save` | 保存本地工作区会话 |
| `ow s restore` | 恢复本地工作区会话 |
| `ow s clean` | 清理本地工作区会话 |

统一面板负责显式插入和应用级命令；Command Mode 负责按住 F24 时连续执行编辑器内高频 Action。

## 10. 语义理解与导航

### 10.1 定义跳转

将光标放在已索引符号上，按 `F12` 跳转到定义。支持的对象包括 module、package、interface、port、parameter、localparam、typedef、enum、macro、signal 和 instance 等。

跳转依赖当前语义快照。快照过期、符号未解析或文件不属于当前工作区时，操作会显示不可用原因。

### 10.2 双击语义浮窗

双击已解析的符号可打开精简语义卡片。标题显示符号名称与类别，默认摘要按符号类型显示：

- parameter、localparam 和 enum member 的当前生效值及类型；未绑定实例的默认求值明确标注为 Default value。
- 端口的解析类型与 signedness，不显示或推测信号实时值。
- 其他符号的数据类型或宏签名。

点击底部文件名与行号跳转到定义；完整路径保留在提示中。点击 **More details** 展开声明、所属作用域、实例路径、求值来源、表达式、维度、位宽及 interface/modport 等辅助信息，**Less details** 收起。每次打开新符号时默认收起，长内容在卡片内部滚动。

过期、不可用与求值失败状态始终保留在摘要中；过期的值和解析类型不会继续显示。卡片采用圆角、轻阴影、低饱和标题背景，随 Light、Dark 与 Catppuccin 主题切换；名称和代码类型保留等宽字体，操作与说明使用非编辑区字体层级。

普通鼠标 hover 不打开该浮窗。浮窗隶属 ZeroSlack 窗口，切换到其他应用时不应覆盖其他应用；按 Esc、点击关闭按钮或浮窗外部可关闭。

### 10.3 数字字面量浮窗

双击 SystemVerilog 数字字面量可查看等效二进制、八进制、十进制和十六进制表示。转换使用 SystemVerilog 位宽、符号位和四态值语义，不按普通字符串转换。

### 10.4 Ghost Annotation

编辑器可在代码行尾显示语义注解：

- 模块例化端口连接可显示形参的原始定义，即 direction、type、dimensions 和 name。
- parameter、localparam 和 enum 可显示经语义计算后的生效值。
- 位宽、数组元素数、实例数等可在有附加信息时显示。
- 源码已明确写出位宽时，重复的简单位宽注解会被抑制。

`Settings > Annotations` 可启用/禁用注解，并限制每行注解数和 lane 数。

### 10.5 Macro 和条件编译

ZeroSlack 使用工作区 include dirs、defines 和源文件中的预处理指令建立宏语义，支持 macro 定义跳转、引用索引、宏值解释、undefined macro 诊断和 inactive `` `ifdef/`ifndef `` 分支灰显。

ZeroSlack 不自动导入 Vivado `.xpr` 或 Tcl 中的宏配置。需要将用于 ZeroSlack 分析的外部 define 写入 Workspace Configuration。

## 11. Diagnostics 与 Problems

Problems 面板汇总 Slang 和 ZeroSlack 语义服务的 error、warning 和 info。

- 可按当前文件/全部文件和严重级别过滤。
- 显示当前文件诊断计数。
- 分析条带标明 `current`、`stale`、`background` 等来源状态。
- 双击 Problems 项可打开文件、定位并短暂高亮诊断位置。
- `F8` 跳到下一条诊断，`Shift+F8` 跳到上一条诊断。

编辑器使用红色/黄色精确下划线标记 error/warning，行号左侧显示诊断图标，鼠标停留可查看原因，右侧滚动区显示对应颜色的位置标记。
package 声明不会因未定义时间刻度而提示 `MissingTimeScale`；模块的相同诊断仍会显示。

## 12. 搜索与安全替换

### 12.1 文件内搜索

`Ctrl+F` 与 `Ctrl+H` 复用编辑器内的查找替换栏，保留查询内容。`Ctrl+Shift+F` 与 `Ctrl+Shift+H` 打开底栏的工作区搜索与替换。Search 不再常驻。

### 12.2 Search and Replace 面板

`Ctrl+Shift+F` 或 `View > Search and Replace` 打开范围搜索。

| 范围 | 含义 |
| --- | --- |
| `Current Syntax Block` | 当前 Tree-sitter 语法块 |
| `Current Module` | 当前 module |
| `Current File` | 当前文件 |
| `Workspace` | 当前工作区 |

搜索可选 Case sensitive、Whole word 和 Semantic。结果按文件和位置展示，可跳转或在临时编辑窗打开。

范围替换使用勾选清单和 High-risk Diff 预览：

1. 选择要修改的文件和匹配项。
2. 输入替换文本，点击 `Build Diff Preview`。
3. 检查 diff，然后执行 `Apply Confirmed Diff`；可使用 Dry run 只验证事务。
4. 搜索结果或文档版本变化后，旧预览会失效，必须重新生成。

## 13. Formatter

ZeroSlack 固定使用 Structured 格式化规则，依据语法结构统一缩进、空格、
换行和对齐，不提供简化格式化档位。

- `Format Document` 格式化当前文档，默认快捷键为 `Ctrl+Shift+I`。
- 编辑器右键菜单不提供 Formatter 入口，避免与快捷键重复。
- 格式化作为单个撤销事务，并恢复逻辑光标。
- 缩进 Tab 转换为 4 个空格。
- module、assign、always/always_ff/always_comb 等顶层结构顶格；module 普通参数、`parameter type`、端口和实例连接按类别对齐。
- Formatter 的编辑范围限于 whitespace 和换行，不应改写 SystemVerilog 标识符、运算符或其他语义 token。

## 14. 折叠

Tree-sitter 语法树提供 module、package、interface、always、case、begin-end 等结构的折叠区。

自定义折叠已移除。已有文件中的 `// fold`、`// endfold` 标记作为普通注释保留，不再生成折叠区。

## 15. RTL Insights

RTL Insights 是面向当前选中符号、模块或代码区域的结构化分析面板。如果语义快照过期，入口会显示分析或保存提示。

Module Brief、Signal Journey 和 Clock/Reset Map 的旧报表页面已移除；以下四类专用图表保留。

### 15.1 Signal Usage Hotspot

Usage Hotspot 提供两种视图：

| 视图 | 用途 |
| --- | --- |
| `Track` | 按模块/文件行范围显示信号读、写、端口、条件、case、timing 等使用位置和相对行号 |
| `Matrix` | 以模块/文件为行、用途类型为列汇总次数，点击单元格查看明细 |

支持搜索、过滤、检查器、源码跳转、缩放、Fit、Center 和布局重置。

### 15.2 FSM Graph

1. 将光标放在结构上被判定为 next-state 的状态变量上。
2. 选择 `State Transition Graph`。选中 current-state 而非 next-state 时，界面会说明需要选择对应 next-state。
3. 图中节点表示状态，边标签使用 `C1`、`C2` 等编号；完整条件、From、To、类型和源位置在表格/检查器中显示。

虚线节点是 canonical 状态的布局别名，用于消除跨多层长边，不增加 FSM 状态统计。仅有自环且无法退出的 dead/end 状态使用特殊标记。节点和边可选中、检查和跳转源码。

FSM 判定基于时序/组合赋值、case 分支和状态传递结构，不以 `cs`、`ns` 等名称作为判定依据。

### 15.3 Module Block Diagram

展示以指定 module 为 top 的模块包含关系。

- 可从当前选中模块设置 top，也可在图中重设 top。
- 可设置展开深度、搜索节点、显示 unresolved instance 和选择紧凑的嵌套/换行布局。
- 图中只显示 module 实例包含关系，不显示端口、信号或 interface。
- 点击节点可查看定义、父节点、路径、深度和解析状态；可跳转到源码。

### 15.4 Signal Kernel Graph

以选中信号为 kernel，将驱动者、kernel 和消费者分区展示。

- 支持高 fanout 折叠/分组、lane、module frame、搜索和过滤。
- enum value 不作为信号节点混入输入/输出分区。
- 可通过节点预览和导航到定义或使用位置。

### 15.5 图形通用操作

侧栏 section 与完整视图共用同一套工作台与同一批专用面板，只是可视面积不同：热点图的 Track/Matrix、
框图的嵌套结构、果核图的过滤与扇出折叠、状态转移图的专用布局与筛选在两处行为一致。容器较窄时，
图形工具栏按控件边界换行，不会压缩按钮文字。

图形视图根据类型支持以下操作：

- 鼠标拖动平移，滚轮或工具按钮缩放。
- Fit、Center Current 和 Reset Layout。
- 搜索、选中、hover inspector、Jump 和 Focus。
- 对支持的图导出 SVG、PDF 或高分辨率 PNG。

## 16. 安全 RTL 修改

这类功能会修改一个或多个 SystemVerilog 文件。它们使用统一的 High-risk + Diff 流程：语义预检、生成修改计划、展示 diff、检查文档版本/冲突、用户确认后原子应用。快照过期或层级上下文不唯一时不会猜测修改。

### 16.1 Rename RTL Port or Parameter

在支持的语义符号上按 `Ctrl+R`，紧凑编辑框会预填当前名称。修改后按 Enter 执行，按 Esc 取消。

支持 wire、logic、reg、enum 类型/变量/值、struct 类型/变量/成员、typedef、module/interface port、parameter 和 localparam。Enter 会先完成语义 generation、文档版本和修改计划校验，再将变更作为一个可撤销事务应用到所有受影响文档缓冲区。应用后会按精确文档 revision 自动刷新内存中的语义结果，可直接对下一个符号再次使用 `Ctrl+R`，不需要先 `Ctrl+S`；无法证明唯一绑定时明确拒绝。

### 16.2 Synchronize Instance Connections

选择目标 module 后使用该 Action，可在一个 修改预览 事务中预览：

- 将按位置连接转换为命名连接。
- 为缺失的 formal port 增加空实参或同名实参。
- 删除 module 已不存在的旧命名端口连接。
- 在位宽和符号性可证明时插入 cast。
- 将同一 module 的所有可证明源码实例一起同步。

默认启用补齐、清理和全实例同步。计划必须通过当前语义 generation、文档版本和源码实例路径校验；任一实例不安全时不提交部分结果。

### 16.3 Connect Instance Pair

1. 在 active top 下选择两个具体 module instance。
2. 打开并排框图，检查两个实例的结构。
3. 从源实例信号拖到目标实例。
4. 预览源模块 output、目标模块 input、父层连接信号和两侧实例连接的统一 diff。

### 16.4 Expose Signal to Top

在已分析的内部信号上使用右键 `Expose Signal to Top...`。ZeroSlack 沿 active hierarchy 为每层增加端口和实例连接，直到 active top，并以一个原子 diff 提交。

该入口只在符号可解析、语义快照当前、active top 存在且实例路径唯一时可用。

### 16.5 Propagate Selected Signals

先通过 `select signals` 复选至少两个信号，再使用 `Propagate Selected Signals...`。可选择目标 ancestor 和 port group，然后一次预览所有信号的跨层端口/连接修改。

### 16.6 Create Signal Definition

将光标放在未定义信号上，使用右键 `Create Signal Definition...`。

- 对 `.port(signal_undeclared)` 这类命名端口实参，从 formal port 的类型、位宽和维度推导候选声明。
- 对 always/always_ff/always_comb 中的未定义程序赋值对象，在当前 module 合法声明区生成候选。
- 先打开靠近源码的小型编辑窗，用户可修改完整声明，然后确认插入。
- 如果文档或语义上下文在确认前变化，候选会失效，不会应用旧修改。

## 17. Settings

左侧栏顶部设置图标（或工程命令 `View > Settings`）在中央标签页打开设置中心。除主题外，设置可选择全局或工作区作用域。

| 分类 | 可配置项 |
| --- | --- |
| `Appearance` | Light / Dark 全局主题，以及 Catppuccin Latte / Frappe / Macchiato / Mocha；主窗口、菜单、对话框、浮窗、编辑器和图形面板同步切换 |
| `Font` | 字体家族、字号、行高和 ligatures；默认字体为 Maple Mono、字号 15、行高 1.4 |
| `Shortcuts` | 按 Action ID 修改快捷键；空值表示禁用该 Action 的快捷键 |
| `Annotations` | Ghost Annotation 开关、每行最大数和最大 lane 数 |
| `Analysis` | 语义分析开关、增量分析开关和诊断数量上限 |
| `Integration` | Pinloom 可执行文件；留空表示从同级正式包、`PINLOOM_APP_PATH` 或 `PATH` 自动发现 |
| `Layout` | 是否恢复工作区会话、是否记住面板状态 |

主题是应用级设置，切换后立即作用于所有 ZeroSlack 窗口。

## 18. 快捷键速查

| 快捷键 | 功能 |
| --- | --- |
| `F24` | 按住进入 Command Mode |
| `Ctrl+Space` | 统一 Symbols / Templates / Commands 面板 |
| `Ctrl+K, Ctrl+O` | 打开工作区 |
| `Ctrl+N` / `Ctrl+O` / `Ctrl+S` / `Ctrl+Shift+S` | 新建 / 打开 / 保存 / 另存为 |
| `F12` | 跳转定义 |
| `Ctrl+G` | 跳转行 |
| `F8` / `Shift+F8` | 下一条 / 上一条诊断 |
| `Alt+F7` / `Shift+Alt+F7` | 下一个 / 上一个信号赋值 |
| `Alt+F8` / `Shift+Alt+F8` | 下一个 / 上一个条件编译分支 |
| `Ctrl+D` | 复制选区 / 当前行 |
| `Ctrl+Shift+L` | 选中当前作用域的所有同符号 |
| `Ctrl+W` | 扩展结构选区 |
| `Ctrl+E` / `Ctrl+Q` | 下一个 / 上一个已选符号 |
| `Ctrl+Shift+D` | 删除整行 |
| `Ctrl+Shift+J` | 合并行 |
| `Alt+Up` / `Alt+Down` | 上移 / 下移行；长按连续移动 |
| `Ctrl+/` / `Ctrl+Shift+/` | 注释 / 取消注释 |
| `Ctrl+]` / `Ctrl+[` | 缩进 / 减少缩进 |
| `Ctrl+Shift+I` | Format Document |
| `Alt+C` | Column Number Tool |
| `Ctrl+R` | Rename RTL Port or Parameter |
| `Ctrl+1` | 显示/隐藏 Navigation |
| `Ctrl+2` | 显示/隐藏 Context 侧栏 |
| `Ctrl+Shift+F` | Search and Replace |
| `Ctrl+J` | 收起/恢复底部面板 |
| `Ctrl+Shift+T` | 重新打开最近关闭的标签页 |
| `Shift+鼠标滚轮` | 水平滚动编辑器；`Ctrl+Shift+鼠标滚轮` 保持字体缩放 |

所有已注册快捷键都可在 `Settings > Shortcuts` 按 Action ID 修改。`help` Action Catalog 用于查看当前构建中的完整 Action 清单、可用条件和所有入口。

## 19. 功能可用性与常见状态

| 现象 | 含义与处理 |
| --- | --- |
| Action 显示 `Semantic snapshot is stale` | 仅改注释、空白或缩进时，停止编辑约 250 ms 后会经后台校验自动恢复语义功能，位置同步到未保存文本；结构、标识符或语法错误改动仍需按 `Ctrl+S` 并等待分析完成。无基准快照或语义分析被禁用时不会自动刷新 |
| 定义、图形或 RTL Action 不可用 | 检查光标是否位于已索引符号、active top 是否正确、工程配置是否包含所需 include/define |
| Design 树缺少实例 | 检查 top、文件扩展名、忽略目录、include dirs 和 Slang 诊断 |
| `;cmd` / `;;cmd` 未弹出候选 | 这是预期行为；使用 `Ctrl+Space`，再选择 Symbols 或 Templates |
| 模块例化模板信息不完整 | 确认语义快照为 current；无法获取目标 module 参数/端口时使用既有保守模板 |
| 高风险预览在确认前失效 | 源文件、快照 generation 或工作区上下文已变化；重新发起 Action，不要复用旧 diff |
| 对没有改动的文件按 `Ctrl+S` 没有任何反应 | 这是预期行为：文件未改动、内容版本与已保存版本一致、且磁盘文件未被外部修改时，保存是真正的空操作，不重写文件，也不触发任何语义分析 |
| 保存后语义分析的范围忽大忽小 | 保存会先判定这次改动的语义影响，再只调度必要的文件与依赖，并且都在 UI 线程之外执行。只改注释与空白（trivia）时走位置重映射通道，完全不调用 Slang；改到声明、端口、package 或宏时才会扩展到依赖它的文件 |


## 20. Workspace Hub 与跨应用上下文

Workspace Hub 按 Source、Pinloom、Wave、RegMap 分组展示当前工作区的资源、状态和预览。
选择、分组展开及布局会持久化；缺失的应用只影响对应分组。编辑外部资源时通过稳定链接交给所属应用。
只读 AI 入口 `zeroslack-cli suite-context` 使用显式引用及有界输出，详见 [integrations](docs/integrations.md)。
Wave 与 RegMap 项目仍由各自的独立应用处理，ZeroSlack 不再提供内置仿真或波形页。

## 21. 编辑行为边界

F24 命令搜索仅由 Enter 执行结果，松开 F24 关闭搜索；空查询时的直接按键动作使用独立的按下/释放规则。
Ctrl+Space 的类型选择器只有带空格前缀时才过滤类型，普通标识符仍是搜索文本。
Ctrl+R 使用语义身份与引用构建重命名计划，不沿用早期按单词替换的 MVP 行为。
括号包裹在选区存在时只包裹选区；没有选区时采用保守结构边界。词法移动和多光标编辑复用统一边界规则。
上述功能的快捷键细节仍以本手册对应章节与当前命令注册为准。
