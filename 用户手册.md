# ZeroSlack 用户手册

> 适用版本：`0.3.0`
> 本手册以当前代码中已注册的功能、入口和默认快捷键为准。

## 1. 产品定位

ZeroSlack 是面向 SystemVerilog/FPGA 开发的图形化代码编辑器。它将日常文本编辑、工程语义分析、RTL 结构查看和可预览的高风险修改放在同一工作区中。

ZeroSlack 对代码的两类认识需要区分：

| 状态 | 数据来源 | 适用功能 |
| --- | --- | --- |
| 当前编辑缓冲区结构 | Tree-sitter 增量语法树 | 高亮、折叠、结构选择、作用域判断、模板插入位置 |
| 已保存工程语义 | Slang 语义快照 | 类型、位宽、生效值、定义跳转、诊断、层级、图形分析和安全 RTL 修改 |

未保存的代码可以继续编辑和使用语法功能，但语义结果可能显示为 `stale`。`Ctrl+S` 保存文件后会排队执行增量语义分析。

## 2. 快速开始

1. 使用 `Ctrl+K, Ctrl+O` 打开工作区，或按 `Ctrl+Space` 切到 Commands 后输入 `ow 1`。
2. 在 `Workspace > Workspace Configuration` 中设置 include 目录、defines、忽略目录、文件扩展名和 active top。
3. 在 Navigation 的 `Files` 页打开文件，在 `Design` 页查看模块实例层级。
4. 编辑后使用 `Ctrl+S` 保存并更新语义快照。
5. 将光标放在符号上，可使用 `F12`、右键菜单中的图形分析，或按住 `F24` 进入 Command Mode。

## 3. 主界面

### 3.1 顶部菜单

| 菜单 | 主要内容 |
| --- | --- |
| `Workspace` | 打开和切换工作区、关闭当前工作区、工程配置、上一条/下一条诊断 |
| `View` | Navigation、搜索替换、焦点模式、编辑器分栏、标签页管理、底部面板、RTL Insights、Signal Kernel Graph、Wave Preview、Fold Shelf、Settings |
| `Tools` | 用户模板、高风险 RTL Actions、崩溃恢复审查 |

### 3.2 Navigation

Navigation 可通过 `View > Navigation` 或 `Ctrl+1` 显示/隐藏。

| 页面 | 功能 |
| --- | --- |
| `Files` | 按文件名或路径过滤；显示工作区文件和虚拟源文件组；可隐藏与当前设计无关的文件 |
| `Design` | 按实例名或模块名过滤；显示自动推断或用户指定的 top 下的模块实例层级 |

`Files` 右键菜单支持新建文件、新建目录、重命名预览、移至回收站预览、复制完整路径和在文件资源管理器中显示。

`Design` 右键菜单支持跳转到实例化位置、跳转到模块定义和设为 Design Top。Design 树仅作为模块层级浏览器，不展示 interface 节点。

### 3.3 底部面板

底部面板可同时容纳 Problems、Activity/Output、搜索结果、RTL Insights、Signal Kernel Graph 和 Wave Preview。

- `Ctrl+J` 收起或恢复底部面板。
- 页面可固定，也可关闭当前页。
- RTL Insights、Signal Kernel Graph 和 Wave Preview 可进入独立焦点视图，然后返回编辑器。
- `View > Reset Panel Layout` 恢复默认面板布局。

## 4. 工作区与工程配置

### 4.1 多工作区

ZeroSlack 可同时打开多个工作区。每个工作区拥有独立的文件索引、语义快照、Design 树、导航历史、配置和会话状态。

可在 `Workspace` 菜单中切换或关闭工作区，也可在统一面板的 Commands 类别使用 `ow` 域。

### 4.2 Workspace Configuration

| 配置 | 用途 |
| --- | --- |
| `Include Dirs` | 指定 `` `include `` 搜索路径 |
| `Defines` | 为当前 ZeroSlack 分析配置提供宏名和可选值 |
| `Ignored Dirs` | 工作区扫描时排除目录 |
| `File Extensions` | 指定纳入工程的 SystemVerilog/Verilog 文件扩展名 |
| `Top Module / Active Top` | 指定层级分析、实例上下文和高风险跨层修改的顶层 |
| `Virtual Source Groups` | 建立具名、有顺序的虚拟文件组，用于组织和分析文件集 |

可移植的工程配置保存在工作区内的 `.zeroslack/project.json`，路径尽量按工作区相对路径保存。

### 4.3 工作区会话

会话状态包括打开的文件、光标位置、标签页、分栏、面板布局和扫描缓存。它保存在本机应用数据目录，不写入源码树。

| Commands 命令 | 作用 |
| --- | --- |
| `ow s save` | 立即保存当前工作区会话 |
| `ow s restore` | 恢复本机会话；需要时只读导入旧 `.zs` |
| `ow s clean` | 清理当前工作区的本机会话，不删除 `project.json` 或旧 `.zs` |

`.zs` 已是兼容用的只读迁移来源，ZeroSlack 不再将新会话写回 `.zs`。

## 5. 文件、标签页与视图

### 5.1 基本文件操作

| 操作 | 默认快捷键 |
| --- | --- |
| 新建文件 | `Ctrl+N` |
| 打开文件 | `Ctrl+O` |
| 保存 | `Ctrl+S` |
| 另存为 | `Ctrl+Shift+S` |
| 重新打开最近关闭的标签页 | `Ctrl+Shift+T` |

当其他应用修改已打开的文件时，ZeroSlack 会跟踪外部变化。无本地未保存修改时可同步内容；存在冲突时需先处理本地内容与磁盘内容的选择。

### 5.2 标签页

标签页支持关闭、关闭其他页、关闭右侧页、关闭全部、锁定、复制视图和在 Context Peek 中打开。

标签页可按以下方式分组：

- `None`：不自动分组。
- `Module`：按 SystemVerilog 模块分组。
- `Workspace`：按工作区分组。

同一文件的复制视图共享同一份 Document，适合在不同位置同时编辑。

### 5.3 编辑器分栏

`View > Editor Layout` 支持：

- 向左、向右、向上、向下新建分栏。
- 最大化当前分栏。
- 均分分栏尺寸。
- 合并当前分栏。
- 拖放标签页时显示落点预览。

`Ctrl+K, Z` 切换 Focus Mode，临时隐藏非编辑区内容。

### 5.4 Context Workspace

`Open in Temporary Editor` 会在编辑区右侧的 Context Peek 中打开目标，
不新增分栏，也不改变现有分栏结构。右侧 Context Rail 是统一入口。

- Peek 只保留一个临时内容；再次打开会替换当前 Peek。
- 搜索框可打开文件、module、package 或符号，Back/Forward 在该视图的
  历史位置间移动。
- Pin 按钮把当前内容原样移入右侧 Context Dock，不会重建编辑器或丢失
  光标状态；Dock 支持多个标签页和原生 Qt 拖动、浮动与尺寸调整。
- Unpin 把当前 Dock 标签页原样移回 Peek；`Esc` 或关闭按钮关闭 Peek。
- 临时编辑器使用共享 Document，与普通标签页共享文本、撤销状态和文件
  身份。关闭 Context View 不会关闭主编辑器中的文件。
- 工作区会恢复已固定内容、标签顺序和活动标签。工作区目录移动后，位于
  工作区内的文件仍按相对路径恢复；临时 Peek 默认不恢复。

### 5.5 崩溃恢复

ZeroSlack 保存未保存文档的恢复信息。`Tools > Review Crash Recovery` 可查看、比较、恢复或丢弃候选内容。

## 6. 日常编辑

### 6.1 标准操作

| 操作 | 默认快捷键 | 说明 |
| --- | --- | --- |
| 撤销 / 重做 | `Ctrl+Z` / `Ctrl+Y` | 按编辑事务撤销或重做 |
| 剪切 / 复制 / 粘贴 | `Ctrl+X` / `Ctrl+C` / `Ctrl+V` | 无选区时，剪切和复制作用于当前整行 |
| 全选 | `Ctrl+A` | 选中当前文档 |
| 文件内查找 / 替换 | `Ctrl+F` / `Ctrl+H` | 当前文件文本查找与替换 |
| 复制选区 / 当前行 | `Ctrl+D` | 有选区时复制精确选区并选中新副本；无选区时复制当前逻辑行 |
| 删除整行 | `Ctrl+Shift+D` | 删除光标或选区触及的所有逻辑行 |
| 合并行 | `Ctrl+Shift+J` | 合并选中行，或当前行与下一行 |
| 上移 / 下移行 | `Alt+Up` / `Alt+Down` | 整行移动光标或选区触及的行；支持长按连续移动，光标跟随被移动内容 |
| 注释 / 取消注释 | `Ctrl+/` / `Ctrl+Shift+/` | 作用于当前行或选中行 |
| 缩进 / 减少缩进 | `Ctrl+]` / `Ctrl+[` | 作用于当前行或选中行 |

Tab 默认输入 4 个空格。内联命令候选和 Slot Mode 打开时，Tab 由当前模式处理。

右键菜单保留与当前选区或语义对象直接相关的操作。`Toggle Selection Case` 可切换选区内字母大小写；`Replace Selection with Spaces` 将普通选区中的非换行字符替换为等量空格，也可将矩形列选区替换为等宽空格，操作均可一次撤销。Replace、注释、取消注释、缩进和减少缩进使用上表快捷键，不在右键菜单重复显示。

光标位于 module 内时，右键菜单的 `Organize Signal Declarations` 会把当前 module 的完整顶层 net/variable 声明集中到安全的声明区。import、typedef、parameter/localparam 等依赖声明仍保留在信号声明之前；原有声明顺序及相邻注释保持不变；always/task/function 等过程块内的局部变量不会移动。当前 module 存在语法错误、条件编译边界或无法确定的结构时，该操作禁用并显示原因。

### 6.2 结构选择与多光标

| 操作 | 默认快捷键 |
| --- | --- |
| 增加下一个同符号出现位置 | Command Mode：`select next occurrence` |
| 选中最近词法作用域内的所有同符号 | `Ctrl+Shift+L` |
| 扩展结构选区 | `Ctrl+W` |
| 移到下一个 / 上一个已选符号 | `Ctrl+E` / `Ctrl+Q` |

Command Mode 中的 `select begin end` 选中最近 `begin-end` 内部的完整行，不包含 `begin` 和 `end` 所在行。

鼠标单击符号仅移动光标；双击符号启用当前文档中的同名符号高亮；在同一位置继续第三次单击会选中完整逻辑行。非末行选区包含换行符，便于整行复制、移动或删除。下一次普通单击会清除同名符号高亮。

### 6.3 列选择和虚拟列

- `Shift+Alt+鼠标拖动` 创建或调整矩形列选区；
  `Shift+Alt+方向键` 可从普通光标直接建立并扩展列选区。
- 列选区已建立后，`Shift+Left` / `Shift+Right` 可直接扩展或收缩列范围，无需继续按住 Alt。
- 列选区支持输入、删除、退格、复制、剪切、粘贴、Tab、Shift+Tab 和方向键。
- 普通单击和方向键不会进入超出行尾的虚拟列。
- `Alt+单击行尾外` 显式进入 Virtual Column；状态提示会标明虚拟列，输入时自动物化所需空格。
- 在行尾外普通单击可保留一个待用虚拟起点，之后的 `Shift+Alt` 列选择可使用该起点。
- `Esc` 退出虚拟列或多光标状态。

### 6.4 Column Number Tool

先建立列选区，再按 `Alt+C` 打开数字序列工具。可配置：

- 十进制、十六进制、八进制或二进制。
- Plain、C-like、SystemVerilog 无位宽或 SystemVerilog 有位宽表示。
- 初始值、步进、重复次数、升序/降序、数字宽度、空格/零填充和十六进制大小写。
- 插入或替换列选区，提交后作为单个撤销事务。窗口会记住上次提交的进制、初始值、步进、重复次数和填充等配置，不显示预览区。

### 6.5 信号复选和赋值队列

1. 在 Command Mode 中执行 `select signals`。
2. 用鼠标左键点击或拖动勾选标识符。
3. 右键结束勾选阶段并打开操作菜单；`Esc` 取消。
4. 选择 `Create Assignment Queue`，生成 `signal <= ;` 队列，所有右值自动进入 Slot Mode。

选中的多个信号也可作为 `Propagate Selected Signals` 的输入。

## 7. 补全、模板和 Slot Mode

### 7.1 统一插入面板

按 `Ctrl+Space` 显式打开统一面板。编辑器获得焦点时默认进入 `Symbols`，其他位置默认进入 `Commands`。

- 直接输入过滤当前类别；方向键移动候选，Enter 执行。
- Tab / Shift+Tab 在 `Symbols`、`Templates`、`Commands` 之间循环。
- `Esc` 关闭面板，不修改编辑器内容。
- 旧的 `;cmd` / `;;cmd` 不再触发补全；分号按普通源码字符处理。

### 7.2 Symbols

`Symbols` 默认搜索当前编辑位置可见的全部可插入语义符号，包括 port、wire、reg、logic、parameter/localparam、typedef/enum/struct 类型与变量、enum value、instance、task 和 function。候选受当前 module、package、词法作用域和 import 约束，不会把其他模块内部信号混入当前列表。输入内容可持续进行模糊过滤。

类型选择器仅用于收窄默认结果。可在过滤文本开头输入选择器和空格，例如 `w `、`r `、`l `、`p `、`lp `、`t `、`f `、`e `、`s ` 和 `td `。`m <filter>` 专门搜索当前工作区的 module，例如 `m uart`。

### 7.3 Templates

`Templates` 提供以下结构化插入：

- logic、wire、reg、parameter、localparam、assign、always、always_comb、always_ff、module、interface、task、function、enum、packed/unpacked struct 和 define/ifdef 等内置模板。
- 全局和工作区用户模板。
- 基于语义索引生成的完整 module 例化，包括命名 parameter 与 port，并进入 Slot Mode。
- package import；插入位置由 Tree-sitter 语法结构决定。
- header include，以及在当前文件目录优先创建新 header 后插入 include。

模板按当前语法位置过滤；不能可靠确定结构或语义时，操作明确拒绝或采用既有保守模板，不跨作用域猜测。

### 7.4 用户模板

`Tools > User Templates` 可打开全局或工作区模板文件，并手动重载。

- 全局模板文件：`user_templates.json`。
- 工作区模板文件：`.zeroslack/user_templates.json`。
- 工作区模板覆盖同名全局模板。
- 用户模板不能覆盖内置命令，也不能占用 `;;h` 和 `;;pk`。

文件格式为 `{"templates": [...]}`。每条记录使用 `command`、`description`、`body` 或 `insertText`，可选提供相对位置 `slots`。重载时会报告无效命令、冲突和 slot 错误。

### 7.5 Slot Mode

模板、语义 module 例化、`clear right`、赋值队列、Package Tools 和 `Edit Instance Slots` 都可进入 Slot Mode。

- 所有 slot 使用可见的周期性提示标记。
- Tab 移到下一个 slot，Shift+Tab 移到上一个 slot。
- 前后移动在 slot 列表中循环。
- `Esc` 完成并退出 Slot Mode。
- `Edit Instance Slots` 可对已有模块实例的参数实参和端口实参重新进入 slot 编辑。

### 7.6 关键字 Ghost Completion

输入 SystemVerilog 关键字前缀时，编辑器可以 ghost 文本显示建议；Tab 接受，`Esc` 或移动光标取消。该功能不恢复旧的无条件通用补全窗。

## 8. Command Mode

Command Mode 用于执行高频编辑和导航 Action，不需要记忆短缩写。

### 8.1 使用方式

1. 按住 `F24`。可在操作系统或键盘重映射中将 CapsLock 等按键映射为 F24。
2. 输入命令全称或有序缩写。例如 `goendm` 可匹配 `go endmodule`，`gm` 可匹配 `go module`。
3. 按 Enter 执行当前命令。
4. 如果 F24 仍被按住，可直接输入下一条命令。
5. 松开 F24 或按 `Esc` 返回编辑器。

普通反引号 `` ` `` 是代码输入，不作为 Command Mode 开关。

### 8.2 编辑与导航命令

| 命令 | 作用 |
| --- | --- |
| `go <number>` | 跳到当前 module 内指定的 1-based 行；`g100`、`go100`、`go 100` 均可解析 |
| `go module` | 打开工作区 module 模糊搜索器 |
| `go package` | 打开工作区 package 模糊搜索器 |
| `go endmodule` | 跳到当前 module 的最后一个 `endmodule` |
| `clear right` | 清空选中行内 assignment 的右值，并将每个右值设为 slot |
| `delete lines` | 删除选区触及的整行 |
| `join lines` | 合并选中行或当前行与下一行 |
| `move lines up` / `move lines down` | 整行移动 |
| `select begin end` | 选中最近 begin-end 块内容 |
| `select signals` | 进入语义信号复选模式 |
| `select next occurrence` | 选中当前符号并增加下一出现位置 |
| `select scope occurrences` | 选中最近词法作用域中的所有同符号 |
| `expand selection` | 逐级扩展到符号层级或外层表达式 |
| `column number` | 打开 Column Number Tool |
| `repeat action` | 重复上一个成功 Action；高风险 Action 会重新进入 dry-run 预览 |
| `help` | 打开 Action Catalog，查看全部 Action、入口和别名 |

Command Mode 还可执行标签页、分栏、面板、Fold Shelf 和高风险 RTL Action。新建、打开、保存、另存为和打开工作区保留菜单/快捷键入口，但不显示在 F24 中。以 `help` 中当前显示的注册项为最终清单。

## 9. Commands

`Ctrl+Space` 后切换到 `Commands`，可执行应用级命令。工作区与 Fold Shelf 命令仍采用域前缀：

| 命令 | 作用 |
| --- | --- |
| `ow <N>` | 一次选择并打开 N 个工作区 |
| `ow r` | 打开最近工作区列表 |
| `ow s save` | 保存本地工作区会话 |
| `ow s restore` | 恢复本地工作区会话 |
| `ow s clean` | 清理本地工作区会话 |
| `fd r` | 进入 Fold Region，标记自定义折叠块 |
| `fd s` | 打开 Fold Shelf |

统一面板负责显式插入和应用级命令；Command Mode 负责按住 F24 时连续执行编辑器内高频 Action。

## 10. 语义理解与导航

### 10.1 定义跳转

将光标放在已索引符号上，按 `F12` 跳转到定义。支持的对象包括 module、package、interface、port、parameter、localparam、typedef、enum、macro、signal 和 instance 等。

跳转依赖当前语义快照。快照过期、符号未解析或文件不属于当前工作区时，操作会显示不可用原因。

### 10.2 双击语义浮窗

双击已解析的符号可打开临时语义浮窗。根据符号类型，浮窗可显示：

- 完整声明、符号类型和所属作用域。
- parameter、localparam 和 enum value 在当前编译/实例上下文中的生效值。
- 数据类型、signedness、packed/unpacked 维度和位宽。
- interface 及 modport；未指定 modport 时显示 interface 本身。
- enum 类型与枚举成员信息。

普通鼠标 hover 不打开该浮窗。浮窗隶属 ZeroSlack 窗口，切换到其他应用时不应覆盖其他应用。

### 10.3 数字字面量浮窗

双击 SystemVerilog 数字字面量可查看等效二进制、八进制、十进制和十六进制表示。转换使用 SystemVerilog 位宽、符号位和四态值语义，不按普通字符串转换。

### 10.4 Ghost Annotation

编辑器可在代码行尾显示语义注解：

- 模块例化端口连接可显示形参的原始定义，即 direction、type、dimensions 和 name。
- parameter、localparam 和 enum 可显示经语义计算后的生效值。
- 位宽、数组元素数、实例数等可在有附加信息时显示。
- 源码已明确写出位宽时，重复的简单位宽注解会被抑制。

`Settings > Annotations` 可启用/禁用注解，并限制每行注解数和 lane 数。

### 10.5 Macro 和条件编译

ZeroSlack 使用工作区 include dirs、defines 和源文件中的预处理指令建立宏语义，支持 macro 定义跳转、引用索引、宏值解释、undefined macro 诊断和 inactive `` `ifdef/`ifndef `` 分支灰显。

ZeroSlack 不自动导入 Vivado `.xpr` 或 Tcl 中的宏配置。需要将用于 ZeroSlack 分析的外部 define 写入 Workspace Configuration。

## 11. Diagnostics 与 Problems

Problems 面板汇总 Slang 和 ZeroSlack 语义服务的 error、warning 和 info。

- 可按当前文件/全部文件和严重级别过滤。
- 显示当前文件诊断计数。
- 分析条带标明 `current`、`stale`、`background` 等来源状态。
- 双击 Problems 项可打开文件、定位并短暂高亮诊断位置。
- `F8` 跳到下一条诊断，`Shift+F8` 跳到上一条诊断。

编辑器使用红色/黄色精确下划线标记 error/warning，行号左侧显示诊断图标，鼠标停留可查看原因，右侧滚动区显示对应颜色的位置标记。

## 12. 搜索与安全替换

### 12.1 文件内搜索

`Ctrl+F` 查找当前文件，`Ctrl+H` 打开当前文件替换。

### 12.2 Search and Replace 面板

`Ctrl+Shift+F` 或 `View > Search and Replace` 打开范围搜索。

| 范围 | 含义 |
| --- | --- |
| `Current Syntax Block` | 当前 Tree-sitter 语法块 |
| `Current Module` | 当前 module |
| `Current File` | 当前文件 |
| `Workspace` | 当前工作区 |

搜索可选 Case sensitive、Whole word 和 Semantic。结果按文件和位置展示，可跳转或在临时编辑窗打开。

范围替换使用勾选清单和 High-risk Diff 预览：

1. 选择要修改的文件和匹配项。
2. 输入替换文本，点击 `Build Diff Preview`。
3. 检查 diff，然后执行 `Apply Confirmed Diff`；可使用 Dry run 只验证事务。
4. 搜索结果或文档版本变化后，旧预览会失效，必须重新生成。

## 13. Formatter

ZeroSlack 固定使用 Structured 格式化规则，依据语法结构统一缩进、空格、
换行和对齐，不提供简化格式化档位。

- `Format Document` 格式化当前文档，默认快捷键为 `Ctrl+Shift+I`。
- 编辑器右键菜单不提供 Formatter 入口，避免与快捷键重复。
- 格式化作为单个撤销事务，并恢复逻辑光标。
- 缩进 Tab 转换为 4 个空格。
- module、assign、always/always_ff/always_comb 等顶层结构顶格；module 参数、端口和实例连接按类别对齐。
- Formatter 的编辑范围限于 whitespace 和换行，不应改写 SystemVerilog 标识符、运算符或其他语义 token。

## 14. 折叠与 Fold Shelf

### 14.1 语法折叠

Tree-sitter 语法树提供 module、package、interface、always、case、begin-end 等结构的折叠区。

### 14.2 Fold Region

`Ctrl+Space` 后输入 `fd r`，可在当前编辑器中标记自定义折叠块。

### 14.3 Fold Shelf

`Ctrl+Space` 后输入 `fd s`，或使用 `View > Fold Shelf`。

- 可在编辑器和 Shelf 之间拖放自定义折叠块。
- Shelf 项目支持持久化和跨文件恢复。
- 删除 Shelf 项目前会执行已移动代码块的保护检查；选中项目后可按 Delete。

## 15. Package Tools

当光标位于语法有效的 package 内时，界面显示 Package Tools。

| 工具 | 插入内容 |
| --- | --- |
| Parameter | parameter |
| Localparam | localparam |
| Enum | `typedef enum` |
| Struct | `typedef struct` |
| Packed Struct | `typedef struct packed` |
| Function | function |

插入位置由 package 语法结构决定，不使用纯字符串定位。插入后进入 Slot Mode。

## 16. RTL Insights

RTL Insights 是面向当前选中符号、模块或代码区域的结构化分析面板。如果语义快照过期，入口会显示分析或保存提示。

### 16.1 Module Brief

汇总当前模块的定义、参数、端口、内部成员、实例和相关语义摘要，用于快速识别模块边界。

### 16.2 Signal Journey

以选中信号为中心展示驱动、使用和跨层关系路径。结果可定位回源码。

### 16.3 Signal Usage Hotspot

Usage Hotspot 提供两种视图：

| 视图 | 用途 |
| --- | --- |
| `Track` | 按模块/文件行范围显示信号读、写、端口、条件、case、timing 等使用位置和相对行号 |
| `Matrix` | 以模块/文件为行、用途类型为列汇总次数，点击单元格查看明细 |

支持搜索、过滤、检查器、源码跳转、缩放、Fit、Center 和布局重置。

### 16.4 Clock/Reset Map

识别时钟和复位域，展示域成员、多时钟/复位证据和相关源码位置。

### 16.5 FSM Graph

1. 将光标放在结构上被判定为 next-state 的状态变量上。
2. 选择 `State Transition Graph`。选中 current-state 而非 next-state 时，界面会说明需要选择对应 next-state。
3. 图中节点表示状态，边标签使用 `C1`、`C2` 等编号；完整条件、From、To、类型和源位置在表格/检查器中显示。

虚线节点是 canonical 状态的布局别名，用于消除跨多层长边，不增加 FSM 状态统计。仅有自环且无法退出的 dead/end 状态使用特殊标记。节点和边可选中、检查和跳转源码。

FSM 判定基于时序/组合赋值、case 分支和状态传递结构，不以 `cs`、`ns` 等名称作为判定依据。

### 16.6 Module Block Diagram

展示以指定 module 为 top 的模块包含关系。

- 可从当前选中模块设置 top，也可在图中重设 top。
- 可设置展开深度、搜索节点、显示 unresolved instance 和选择紧凑的嵌套/换行布局。
- 图中只显示 module 实例包含关系，不显示端口、信号或 interface。
- 点击节点可查看定义、父节点、路径、深度和解析状态；可跳转到源码。

### 16.7 Signal Kernel Graph

以选中信号为 kernel，将驱动者、kernel 和消费者分区展示。

- 支持高 fanout 折叠/分组、lane、module frame、搜索和过滤。
- enum value 不作为信号节点混入输入/输出分区。
- 可通过节点预览和导航到定义或使用位置。

### 16.8 Wave Preview

Wave Preview 根据选中或当前 always/module 的代码结构生成静态象征波形草图，展示 lane、条件事件、可推断的变化和 warning，并可跳转到源码。

Wave Preview 不是仿真器，不运行 testbench，不调用 Vivado，也不读取仿真波形数据库。

### 16.9 Wave Simulation

Wave Simulation 是实际运行 Verilator 的独立工作流，不等同于静态 Wave Preview。它从当前 module、always、信号或 Design instance 建立可编辑 stimulus，运行后在编辑区打开完整 WaveWorkbench 结果页。

Windows 正式包已内置 Verilator 5.050、MinGW 13.1 和 GNU Make，目标电脑无需安装编译器，也无需修改系统 `PATH`。可在 `Settings > Simulation` 分别指定外部 Verilator 和 C++ 编译器；路径留空时，ZeroSlack 优先使用安装目录旁的 `WaveWorkbench/toolchain`，再检查 `VERILATOR` / `VERILATOR_ROOT` / `CXX` 环境变量和 `PATH`。探测失败时会分别显示 Verilator 与编译器的具体原因。

### 16.10 图形通用操作

图形视图根据类型支持以下操作：

- 鼠标拖动平移，滚轮或工具按钮缩放。
- Fit、Center Current 和 Reset Layout。
- 搜索、选中、hover inspector、Jump 和 Focus。
- 对支持的图导出 SVG、PDF 或高分辨率 PNG。

## 17. 安全 RTL 修改

这类功能会修改一个或多个 SystemVerilog 文件。它们使用统一的 High-risk + Diff 流程：语义预检、生成修改计划、展示 diff、检查文档版本/冲突、用户确认后原子应用。快照过期或层级上下文不唯一时不会猜测修改。

### 17.1 Rename RTL Port or Parameter

在支持的语义符号上按 `Ctrl+R`，紧凑编辑框会预填当前名称。修改后按 Enter 执行，按 Esc 取消。

支持 wire、logic、reg、enum 类型/变量/值、struct 类型/变量/成员、typedef、module/interface port、parameter 和 localparam。Enter 会先完成语义 generation、文档版本和修改计划校验，再将变更作为一个可撤销事务应用到所有受影响文档缓冲区。应用后会按精确文档 revision 自动刷新内存中的语义结果，可直接对下一个符号再次使用 `Ctrl+R`，不需要先 `Ctrl+S`；无法证明唯一绑定时明确拒绝。

### 17.2 Synchronize Instance Connections

选择目标 module 后使用该 Action，可在一个 High+Diff 事务中预览：

- 将按位置连接转换为命名连接。
- 为缺失的 formal port 增加空实参或同名实参。
- 删除 module 已不存在的旧命名端口连接。
- 在位宽和符号性可证明时插入 cast。
- 将同一 module 的所有可证明源码实例一起同步。

默认启用补齐、清理和全实例同步。计划必须通过当前语义 generation、文档版本和源码实例路径校验；任一实例不安全时不提交部分结果。

### 17.3 Connect Instance Pair

1. 在 active top 下选择两个具体 module instance。
2. 打开并排框图，检查两个实例的结构。
3. 从源实例信号拖到目标实例。
4. 预览源模块 output、目标模块 input、父层连接信号和两侧实例连接的统一 diff。

### 17.4 Expose Signal to Top

在已分析的内部信号上使用右键 `Expose Signal to Top...`。ZeroSlack 沿 active hierarchy 为每层增加端口和实例连接，直到 active top，并以一个原子 diff 提交。

该入口只在符号可解析、语义快照当前、active top 存在且实例路径唯一时可用。

### 17.5 Propagate Selected Signals

先通过 `select signals` 复选至少两个信号，再使用 `Propagate Selected Signals...`。可选择目标 ancestor 和 port group，然后一次预览所有信号的跨层端口/连接修改。

### 17.6 Create Signal Definition

将光标放在未定义信号上，使用右键 `Create Signal Definition...`。

- 对 `.port(signal_undeclared)` 这类命名端口实参，从 formal port 的类型、位宽和维度推导候选声明。
- 对 always/always_ff/always_comb 中的未定义程序赋值对象，在当前 module 合法声明区生成候选。
- 先打开靠近源码的小型编辑窗，用户可修改完整声明，然后确认插入。
- 如果文档或语义上下文在确认前变化，候选会失效，不会应用旧修改。

## 18. Settings

`View > Settings` 打开设置中心。除主题外，设置可选择全局或工作区作用域。

| 分类 | 可配置项 |
| --- | --- |
| `Appearance` | Light / Dark 全局主题；主窗口、菜单、对话框、浮窗、编辑器和图形面板同步切换 |
| `Font` | 字体家族、字号、行高和 ligatures；默认字体为 Maple Mono、字号 15、行高 1.4 |
| `Shortcuts` | 按 Action ID 修改快捷键；空值表示禁用该 Action 的快捷键 |
| `Annotations` | Ghost Annotation 开关、每行最大数和最大 lane 数 |
| `Analysis` | 语义分析开关、增量分析开关和诊断数量上限 |
| `Simulation` | Verilator 和 C++ 编译器可执行文件；留空表示自动发现 |
| `Layout` | 是否恢复工作区会话、是否记住面板状态 |

主题是应用级设置，切换后立即作用于所有 ZeroSlack 窗口。

## 19. 快捷键速查

| 快捷键 | 功能 |
| --- | --- |
| `F24` | 按住进入 Command Mode |
| `Ctrl+Space` | 统一 Symbols / Templates / Commands 面板 |
| `Ctrl+K, Ctrl+O` | 打开工作区 |
| `Ctrl+N` / `Ctrl+O` / `Ctrl+S` / `Ctrl+Shift+S` | 新建 / 打开 / 保存 / 另存为 |
| `F12` | 跳转定义 |
| `Ctrl+G` | 跳转行 |
| `F8` / `Shift+F8` | 下一条 / 上一条诊断 |
| `Alt+F7` / `Shift+Alt+F7` | 下一个 / 上一个信号赋值 |
| `Alt+F8` / `Shift+Alt+F8` | 下一个 / 上一个条件编译分支 |
| `Ctrl+D` | 复制选区 / 当前行 |
| `Ctrl+Shift+L` | 选中当前作用域的所有同符号 |
| `Ctrl+W` | 扩展结构选区 |
| `Ctrl+E` / `Ctrl+Q` | 下一个 / 上一个已选符号 |
| `Ctrl+Shift+D` | 删除整行 |
| `Ctrl+Shift+J` | 合并行 |
| `Alt+Up` / `Alt+Down` | 上移 / 下移行；长按连续移动 |
| `Ctrl+/` / `Ctrl+Shift+/` | 注释 / 取消注释 |
| `Ctrl+]` / `Ctrl+[` | 缩进 / 减少缩进 |
| `Ctrl+Shift+I` | Format Document |
| `Alt+C` | Column Number Tool |
| `Ctrl+R` | Rename RTL Port or Parameter |
| `Ctrl+1` | 显示/隐藏 Navigation |
| `Ctrl+Shift+F` | Search and Replace |
| `Ctrl+J` | 收起/恢复底部面板 |
| `Ctrl+K, Z` | Focus Mode |
| `Ctrl+Shift+T` | 重新打开最近关闭的标签页 |

所有已注册快捷键都可在 `Settings > Shortcuts` 按 Action ID 修改。`help` Action Catalog 用于查看当前构建中的完整 Action 清单、可用条件和所有入口。

## 20. 功能可用性与常见状态

| 现象 | 含义与处理 |
| --- | --- |
| Action 显示 `Semantic snapshot is stale` | 当前文本已超前于已保存语义快照；先按 `Ctrl+S` 并等待分析完成 |
| 定义、图形或 RTL Action 不可用 | 检查光标是否位于已索引符号、active top 是否正确、工程配置是否包含所需 include/define |
| Design 树缺少实例 | 检查 top、文件扩展名、忽略目录、include dirs 和 Slang 诊断 |
| `;cmd` / `;;cmd` 未弹出候选 | 这是预期行为；使用 `Ctrl+Space`，再选择 Symbols 或 Templates |
| 模块例化模板信息不完整 | 确认语义快照为 current；无法获取目标 module 参数/端口时使用既有保守模板 |
| 高风险预览在确认前失效 | 源文件、快照 generation 或工作区上下文已变化；重新发起 Action，不要复用旧 diff |
| Wave Preview 与真实仿真不同 | 它是基于代码的静态草图，不表示仿真时间和真实激励结果 |
